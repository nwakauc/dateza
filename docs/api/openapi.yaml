openapi: 3.1.0
info:
  title: D8N Core API
  version: 1.0.0
  summary: Multi-brand dating platform API
  description: |
    D8N resolves the active brand from the request host. Never send a brand ID in
    client-controlled input. Sessions are brand-bound, and profile identifiers
    exposed by dating endpoints are public UUIDs rather than internal database IDs.

    Browser clients may request `session_mode: browser` at password registration
    or login. D8N then sets a host-only HttpOnly cookie and does not return its
    secret in JSON. Send browser requests with credentials enabled. Every unsafe
    cookie-authenticated request must include `X-CSRF-Token`, obtained from the
    auth response or `GET /api/v1/me`. Bearer mode remains the default and does
    not set a cookie; bearer-authenticated requests do not require a CSRF token.

    ## Implementation status (for frontend integration)

    Every path documented in this contract is implemented and callable. Use this
    summary to see which product domains are complete versus intentionally limited:

    - **Available now:** Identity (register, login, password and login-email
      change, recovery, brand-bound session, identifier verification), Profiles (profile,
      configuration, options, preferences, location, publication), HookUs Matching
      (discovery, likes, passes, matches), DateZA Find (10 unique profiles per
      Johannesburg day) with deterministic DateZA v1 compatibility and
      verification-gated DateZA interactions, and Trust foundations (block,
      report, brand-scoped review and suspension).
    - **Preview (limited scope):** Messaging supports match-gated plain-text
      messages and history. Client idempotency, delivery/read receipts, realtime,
      and message media are not available.
    - **Available:** Profile photos use a direct-to-R2
      upload — request an intent, `PUT` the bytes to the returned short-lived
      presigned URL, then attach the returned `signed_id`. Attached photos begin
      `pending_review`; initial visibility is a per-brand policy — HookUs and
      DateZA photos are `visible` immediately and moderated asynchronously in the
      background, while a brand without an explicit immediate policy stays
      `hidden` until approval. `pending_review` means moderation has not yet run;
      it is never presented as approval or verification. Public delivery positively requires a
      kept, visible, successfully processed safe derivative whose moderation state
      is pending/approved; rejected media is independently excluded. Signed URLs
      are short-lived. The endpoints require private R2 storage, so they return
      `404` when R2 is disabled.
    - **Available foundation:** Admin/HQ uses brand-scoped capability RBAC and
      mandatory TOTP session step-up. Founder, Super Admin, Operations, Trust &
      Safety, Support, Engineering, Marketing, Analyst, and legacy Moderator have
      server-owned capability sets. No role bypasses brand assignment.
    - **Available foundation:** Product Notifications exposes a brand-scoped,
      polling-friendly in-app inbox and read state. DateZA registration produces
      `dateza.welcome`; product email is asynchronous. Device/push storage and a
      provider boundary exist, but device-registration API and a production push
      provider remain gated.
    - **Planned (no consumer product endpoints yet):** RealMe verification,
      Billing, and Analytics.

    DateZA is a provisionable tenant (`DateZA`, slug `dateza`) with phone/email
    password authentication, a v1 profile catalog, and Find with a durable daily
    10-unique-profile Find allowance, a separate stable daily Discovery
    allocation of up to 10, and deterministic `dateza_v1` compatibility on
    sufficiently informed cards. RealMe, public Trust standing, AI Matchmaker,
    notification UI/device enrollment, and subscriptions/entitlements are not
    implemented.

    The live API root (`GET /`) returns the same per-service status machine-readably.
servers:
  - url: /
    description: Current API origin (use this for the self-hosted Swagger UI)
  - url: https://{brandHost}
    description: Production brand-specific API origin
    variables:
      brandHost:
        default: hookus.example.com
security:
  - bearerAuth: []
  - cookieAuth: []
tags:
  - name: System
  - name: Authentication
  - name: Profile
  - name: Matching
  - name: Messaging
  - name: Notifications
  - name: Trust
  - name: Hooks
    description: |
      🔥 Hook — a stronger-intent interaction than a Like. The sender gets exactly
      ONE unsolicited opening message to a recipient and is then locked out until
      the recipient engages. The recipient's first reply IS acceptance: it creates
      a normal Match + Conversation, after which all further chat flows through the
      standard Messaging endpoints. See ADR 0015.
  - name: Hq
    description: |
      D8N HQ — internal company command centre backend (see
      docs/FOUNDER-HQ/D8N-HQ/). Never a consumer-facing API: every operation
      here requires an explicitly capable operator (identical authorization to
      the Admin tag), a verified admin-MFA session unless stated otherwise, and
      is scoped to the request's brand. See
      docs/FOUNDER-HQ/D8N-HQ/SECURITY-AND-RBAC.md for the authorization model
      and audit requirements.
  - name: Hook Tonight
    description: |
      Hook Tonight — a TEMPORARY "I'm available/open to meeting tonight"
      availability signal (distinct from a 🔥 Hook, which says "I specifically
      want you"). Activating surfaces a member in the Hook Tonight discovery pool
      of other compatible, currently-available members; it never creates a match
      or conversation on its own. To approach someone found there, the member
      sends the existing 🔥 Hook. Availability lapses automatically and is checked
      against the clock at read time, so a stale activation never makes anyone
      appear available.
paths:
  /api/v1/health:
    get:
      operationId: getHealth
      tags: [System]
      summary: Check API dependency readiness
      description: |
        Reports whether the web process can reach both the primary PostgreSQL
        database and the Solid Queue database. Use `/up` for boot/liveness and
        this endpoint for readiness. It is intentionally independent of brand
        host resolution and exposes no connection details.
      security: []
      responses:
        "200":
          description: API is available.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
        "503":
          description: One or more required database dependencies are unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
  /api/v1/version:
    get:
      operationId: getVersion
      tags: [System]
      summary: Identify the exact deployed build and release
      description: |
        Public, non-secret release identity. `git_sha` is baked into the image;
        `image_version` is Kamal's runtime image version. A null field means the
        corresponding build input was unavailable and must not be inferred.
      security: []
      responses:
        "200":
          description: Deployed application identity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VersionResponse"
  /api/v1/openapi.json:
    get:
      operationId: getOpenapiContract
      tags: [System]
      summary: Download this API contract as JSON
      security: []
      responses:
        "200":
          description: OpenAPI contract for the deployed commit.
          content:
            application/json:
              schema:
                type: object
  /api/v1/auth/methods:
    get:
      operationId: getAuthenticationMethods
      tags: [Authentication]
      summary: List implemented authentication methods enabled for this brand
      security: []
      responses:
        "200":
          description: Host-resolved brand and currently available methods.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthMethodsResponse"
        "404":
          $ref: "#/components/responses/BrandRequired"
  /api/v1/auth/password/register:
    post:
      operationId: registerWithPassword
      tags: [Authentication]
      summary: Register immediately with a phone or email and password
      description: |
        Creates a D8N identity, current-brand membership, and brand-bound session.
        Registration does not prove control of the phone number or email address;
        the returned identifier remains unverified until a later verification flow.
        Set `session_mode` to `browser` for an HttpOnly persistent web session;
        omit it for the existing bearer response.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordAuthRequest"
      responses:
        "201":
          description: Account, membership, password credential, and session created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasswordAuthSessionResponse"
        "403":
          description: Browser mode was requested from an unapproved origin (`browser_session_origin_not_allowed`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Authentication method or browser-session capability is unavailable for the resolved brand.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The request already carries an authenticated D8N session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Registration cannot be completed with the supplied input, or `session_mode` is unsupported (`invalid_session_mode`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          $ref: "#/components/responses/RateLimited"
  /api/v1/auth/password/login:
    post:
      operationId: loginWithPassword
      tags: [Authentication]
      summary: Log in with an existing phone or email password credential
      description: |
        Login requires an existing active membership in the host-resolved brand.
        It never enrolls an existing D8N identity into another brand implicitly.
        Set `session_mode` to `browser` for an HttpOnly persistent web session;
        omit it for the existing bearer response.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordAuthRequest"
      responses:
        "201":
          description: Brand-bound session issued.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasswordAuthSessionResponse"
        "403":
          description: Browser mode was requested from an unapproved origin (`browser_session_origin_not_allowed`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Identifier, password, account lifecycle, or membership is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Authentication method or browser-session capability is unavailable for the resolved brand.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: |
            The identifier and password are correct, but the brand membership is
            deactivated (`account_deactivated`). Only ever returned after
            password verification, so it cannot be used to enumerate account
            state. Call `POST /api/v1/auth/password/reactivation` with the same
            credentials to restore access.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: The supplied `session_mode` is unsupported (`invalid_session_mode`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          $ref: "#/components/responses/RateLimited"
  /api/v1/auth/password/reactivation:
    post:
      operationId: reactivateAccount
      tags: [Authentication]
      summary: Reactivate a deactivated account and issue a new session
      description: |
        Explicit, credential-verified reactivation of an account deactivated via
        `POST /api/v1/account/deactivation`. Deliberately mirrors login rather
        than a bare confirm button on a stale session: deactivation revokes every
        session for the brand, so restoring access requires re-proving the
        password. Shares the login throttle budget for the identifier (this is
        still fundamentally a credential-verification attempt).
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordAuthRequest"
      responses:
        "201":
          description: Brand membership restored and a new brand-bound session issued.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasswordAuthSessionResponse"
        "401":
          description: Identifier or password is invalid (`invalid_credentials`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Authentication method or browser-session capability is unavailable for the resolved brand.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: |
            The request already carries an authenticated D8N session, or the
            credentials are correct but the membership is not currently
            deactivated (`account_not_deactivated`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          $ref: "#/components/responses/RateLimited"
  /api/v1/account/deactivation:
    post:
      operationId: deactivateAccount
      tags: [Authentication]
      summary: Reversibly deactivate the current user's account for this brand
      description: |
        Brand-level, reversible deactivation. Requires an explicit
        `confirmation: "deactivate"` so it can never be triggered accidentally.
        Immediately revokes every session and device registration for this
        brand (including the caller's) and removes the member from discovery,
        find, matching, and messaging via the same shared eligibility predicate
        used platform-wide — but profile, photos, matches, likes, conversations,
        location, and preferences are left completely intact. Restore access
        with `POST /api/v1/auth/password/reactivation`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountDeactivationRequest"
      responses:
        "200":
          description: The account was deactivated (or was already deactivated).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountDeactivationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: No active membership for this brand (`account_unavailable`), or deactivation is unavailable for the resolved brand.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The membership cannot be deactivated from its current lifecycle state (`account_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Missing or wrong confirmation (`confirmation_required`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/password:
    patch:
      operationId: changePassword
      tags: [Authentication]
      summary: Change the current password after reauthentication
      description: |
        Requires a password-backed brand session and the current password in the
        same request. A successful change keeps the current session and revokes
        every other active session issued from the affected password credential.
        Unauthenticated account recovery through phone or email is not this endpoint.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordChangeRequest"
      responses:
        "200":
          description: Password changed and other affected sessions revoked.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "401":
          description: The bearer session or current password is invalid.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The current session was not issued from a password credential.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: The replacement password is invalid, mismatched, or unchanged.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          $ref: "#/components/responses/RateLimited"
  /api/v1/auth/email/change:
    post:
      operationId: requestEmailChange
      tags: [Authentication]
      summary: Request a verification code for a replacement login email
      description: |
        Requires a bearer session issued from the email/password credential being
        changed plus the current password in this request. The replacement is
        normalized and must not already belong to another D8N identity. D8N sends
        a single-use 10-minute code to the proposed email while leaving the current
        identifier, password credential, and login behavior untouched. Malformed,
        unchanged, and occupied replacement addresses share one
        `email_change_unavailable` response after successful reauthentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailChangeRequest"
      responses:
        "202":
          description: A purpose- and session-bound code was queued for the proposed email.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "401":
          description: The bearer session or current password is invalid (`invalid_current_password`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The current session is not backed by an email/password credential (`password_credential_required`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: The proposed address is malformed, unchanged, or unavailable (`email_change_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          description: The configured email delivery channel is unavailable (`delivery_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      operationId: confirmEmailChange
      tags: [Authentication]
      summary: Confirm and activate a replacement login email
      description: |
        Submit the exact proposed email and its six-digit code from the same
        brand-bound session that requested the change. On success D8N updates the
        existing identity identifier in place, marks the new email verified,
        preserves its password credential and the current session, consumes other
        outstanding challenges for that identifier, and revokes every other active
        session backed by the affected credential across brands. The old address
        remains active until this atomic confirmation succeeds.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailChangeConfirmation"
      responses:
        "200":
          description: The verified replacement email is now the login identifier.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailChangeResponse"
        "401":
          description: Session, proposed email, or code is unavailable or invalid (`verification_code_invalid`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The purpose-bound code has already been consumed (`verification_code_used`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "410":
          description: The purpose-bound code has expired (`verification_code_expired`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: The code exhausted its permitted verification attempts (`verification_attempts_exhausted`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: The proposed email became occupied before confirmation (`email_change_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/password/recovery:
    post:
      operationId: requestPasswordRecovery
      tags: [Authentication]
      summary: Start signed-out password recovery
      description: |
        Step 1 of 3 of the signed-out "Forgot password?" flow. No bearer session is
        required. Submit the account identifier (phone or email); the server sends a
        single-use recovery code to that channel **only** when it maps to an eligible
        identity: an already-verified identifier that owns an active password
        credential and a membership in the resolved brand.

        This endpoint is deliberately account-enumeration resistant. It returns the
        **same** neutral `202` with the same body for every case — identifier
        unknown, unverified, without a password credential, not a member of this
        brand, suspended, closed/left, or rate-limited. It never returns `429`:
        throttling manifests as silent non-delivery so rate-limit behavior cannot be
        used to probe which identifiers exist. The recovery code expires in 10
        minutes. Only a `422 invalid_request` (identifier field absent) differs, and
        that reflects request shape, not account state.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordRecoveryRequest"
      responses:
        "202":
          description: Request accepted. Identical regardless of whether an eligible account exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "422":
          description: The `identifier` field was missing (`invalid_request`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/password/recovery/verify:
    post:
      operationId: verifyPasswordRecovery
      tags: [Authentication]
      summary: Verify a recovery code and obtain a reset authorization
      description: |
        Step 2 of 3. Submit the identifier and the recovery code that was delivered.
        On success the server returns a single-use, short-lived (15 minute) reset
        authorization token used in step 3. The recovery code is consumed on success
        and is attempt-limited (locked after 5 wrong tries).

        Responses preserve enumeration resistance: an unknown identifier, a
        missing challenge, and a wrong code all return the same `401 invalid_code`.
        Expired or consumed lifecycle is disclosed only when the submitted secret
        securely matches the corresponding prior challenge. A reset token is only
        ever returned to a caller that presents the code delivered to the
        identifier's own channel.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordRecoveryVerifyRequest"
      responses:
        "201":
          description: Recovery code accepted; a single-use reset authorization is issued.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasswordResetAuthorization"
        "401":
          description: The identifier/code pair is unknown or invalid (`invalid_code`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: A matching recovery code was already consumed (`verification_code_used`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "410":
          description: A matching recovery code expired (`verification_code_expired`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: A matching recovery code exhausted its permitted attempts (`verification_attempts_exhausted`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/password/recovery/reset:
    post:
      operationId: resetPasswordWithRecovery
      tags: [Authentication]
      summary: Set a new password using a reset authorization
      description: |
        Step 3 of 3. Exchange the reset authorization token from step 2 for a new
        password. The new password must satisfy the same policy as registration and
        password change (≥ 6 characters, ≤ 72 UTF-8 bytes).

        On success the reset token is consumed (replay returns `401`) and **every
        active session issued from the affected password credential is revoked across
        all brands** — the D8N password is a platform-wide identity secret, so
        rotating it invalidates every brand-scoped session it backed. Recovery never
        changes brand-membership state: a suspended or `left`/closed membership stays
        as-is, so a successful reset does not by itself restore the ability to sign
        in to a brand the user was suspended from or has left.

        An invalid/mismatched/short password returns `422 invalid_password` and
        leaves the authorization usable for a retry. An unknown, expired, or already
        used token returns `401 invalid_or_expired_token`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordResetRequest"
      responses:
        "200":
          description: Password reset; affected sessions revoked.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "401":
          description: The reset token is unknown, expired, or already used (`invalid_or_expired_token`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: The new password is invalid, too short, or does not match its confirmation (`invalid_password`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/verification:
    post:
      operationId: requestIdentifierVerification
      tags: [Authentication]
      summary: Request a post-signup phone or email verification code
      description: |
        Requires a brand-bound bearer session. The code is sent only to an
        unverified identifier already owned by the current user. The generic
        success response does not reveal whether that identifier exists or is
        already verified, and requesting a code never creates account state.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierVerificationRequest"
      responses:
        "202":
          description: Request accepted without disclosing identifier state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerificationDeliveryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          $ref: "#/components/responses/InvalidInput"
        "429":
          description: Resend cooldown (`verification_resend_too_soon`) or broader verification throttle (`verification_rate_limited`).
          headers:
            Retry-After:
              schema:
                type: integer
              description: Authoritative seconds until another resend may be attempted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: The configured delivery channel is temporarily unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      operationId: verifyIdentifier
      tags: [Authentication]
      summary: Verify the current user's phone or email identifier
      description: |
        Consumes a purpose-bound code and marks only the current user's matching
        identifier verified. This endpoint never issues a session or creates a
        user, credential, membership, or profile.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierVerification"
      responses:
        "200":
          description: Identifier verified.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdentifierVerificationResponse"
        "401":
          description: Session or verification code is unavailable or invalid (`verification_code_invalid`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The verification code was already consumed (`verification_code_used`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "410":
          description: The verification code expired (`verification_code_expired`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: The verification code exhausted its permitted attempts (`verification_attempts_exhausted`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/session:
    delete:
      operationId: revokeSession
      tags: [Authentication]
      summary: Revoke the current session
      description: Cookie-authenticated logout requires `X-CSRF-Token` and clears the browser cookie.
      responses:
        "204":
          description: Session revoked.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/CsrfRejected"
  /api/v1/me:
    get:
      operationId: getCurrentIdentity
      tags: [Authentication]
      summary: Confirm the authenticated identity, brand, and session
      description: Browser authentication uses the HttpOnly cookie and returns the CSRF token for that session.
      responses:
        "200":
          description: Current authenticated context.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    delete:
      operationId: closeAccount
      tags: [Authentication]
      summary: Close the current user's account for this brand
      description: |
        Brand-level account closure (the user leaves this brand). Requires an
        explicit `confirmation: "close"` so it can never be triggered accidentally.
        Effects are immediate and one-way: the brand membership is tombstoned, the
        profile is discarded and anonymized, active matches end, and **every session
        for this brand is revoked** (including the caller's — a retry with the same
        token returns 401). Profile media is physically purged from object storage
        **asynchronously**; `media_purge_state` starts `pending`. The user's D8N
        network identity (credentials/identifiers) and other brands are untouched —
        platform-wide identity deletion is a separate future capability. Shared and
        safety records (conversations, messages, blocks, reports, enforcements,
        audit) are retained. Re-joining requires fresh registration; closure is not
        reversible through this API.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CloseAccountRequest"
      responses:
        "200":
          description: The account was closed (or was already closed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CloseAccountResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: No active membership for this brand (`account_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Missing or wrong confirmation (`confirmation_required`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/profile/configuration:
    get:
      operationId: getProfileConfiguration
      tags: [Profile]
      summary: Get the resolved brand's onboarding and profile contract
      responses:
        "200":
          description: Brand-owned field/option configuration and current server-owned onboarding state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProfileConfigurationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/profile:
    get:
      operationId: getProfile
      tags: [Profile]
      summary: Get the current brand profile
      responses:
        "200":
          description: Current profile and resumable onboarding state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CurrentProfileResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      operationId: updateProfile
      tags: [Profile]
      summary: Create or update the current brand profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProfileUpdate"
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "200":
          description: Updated owner profile and resumable onboarding state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CurrentProfileResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "422":
          $ref: "#/components/responses/ProfileValidationFailed"
  /api/v1/profile/preferences:
    get:
      operationId: getProfilePreferences
      tags: [Profile]
      summary: Get the current brand preferences
      responses:
        "200":
          description: Current preferences or null.
          content:
            application/json:
              schema:
                type: object
                required: [preferences]
                properties:
                  preferences:
                    oneOf:
                      - $ref: "#/components/schemas/ProfilePreferences"
                      - type: "null"
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      operationId: updateProfilePreferences
      tags: [Profile]
      summary: Create or update current brand preferences
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProfilePreferencesUpdate"
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "200":
          description: Updated preferences.
          content:
            application/json:
              schema:
                type: object
                required: [preferences]
                properties:
                  preferences:
                    $ref: "#/components/schemas/ProfilePreferences"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "422":
          $ref: "#/components/responses/ValidationFailed"
  /api/v1/profile/options:
    patch:
      operationId: replaceProfileOptions
      tags: [Profile]
      summary: Replace selections for supplied controlled-option groups
      description: Omitted groups are unchanged. Supplied group arrays replace that group's active selections.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [selections]
              properties:
                selections:
                  type: object
                  additionalProperties:
                    type: array
                    items:
                      type: string
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "200":
          description: Updated owner profile.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OwnerProfileResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "422":
          $ref: "#/components/responses/ValidationFailed"
  /api/v1/profile/prompts:
    get:
      operationId: getProfilePrompts
      tags: [Profile]
      summary: The member's current prompt answers
      responses:
        "200":
          description: The member's answered prompts, ordered by position.
          content:
            application/json:
              schema:
                type: object
                required: [prompts]
                properties:
                  prompts:
                    type: array
                    items:
                      $ref: "#/components/schemas/PromptAnswer"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
    put:
      operationId: replaceProfilePrompts
      tags: [Profile]
      summary: Replace the member's prompt answers
      description: |
        Replaces the full ordered set of prompt answers. Array order becomes the
        display position. Unknown or duplicate prompt keys are rejected; at most
        five answers are allowed and each answer is length-limited.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [answers]
              properties:
                answers:
                  type: array
                  maxItems: 5
                  items:
                    $ref: "#/components/schemas/PromptAnswerInput"
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "200":
          description: Updated owner profile.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OwnerProfileResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "422":
          $ref: "#/components/responses/ValidationFailed"
  /api/v1/profile/publication:
    post:
      operationId: activateProfile
      tags: [Profile]
      summary: Activate and publish a complete profile
      responses:
        "200":
          description: Activated owner profile.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OwnerProfileResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "422":
          description: Profile does not satisfy current brand completion requirements.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncompleteProfileError"
    delete:
      operationId: deactivateProfile
      tags: [Profile]
      summary: Hide and deactivate the current profile
      responses:
        "200":
          description: Deactivated owner profile.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OwnerProfileResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
  /api/v1/profile/location:
    get:
      operationId: showProfileLocation
      tags: [Profile]
      summary: Authoritative owner readback of the current profile's dating location
      description: |
        The backend is the sole source of truth for whether a dating location is
        configured — clients must not derive this from local/device state. Reads
        the same `ProfileLocation` row every write path (`PUT /profile/location`,
        `PUT /profile/place`) and Matching's distance filtering consume, so this
        endpoint can never disagree with what Discover/Find actually use.

        Never returns latitude/longitude, regardless of `source` — only a safe
        display representation. A missing profile, a profile with no saved
        location, and a soft-deleted location all resolve to the same
        `configured: false` response rather than an error, matching
        `GET /api/v1/profile/preferences`'s null-safe convention. Owner-only:
        there is no way to address another member's location through this
        endpoint, and a suspended profile may still read its own location.

        `configured` here is definitionally identical to the `location.configured`
        summary already returned inline by `GET /api/v1/profile` — the two must
        never disagree, since both derive from the presence of the same kept
        `ProfileLocation` row.
      responses:
        "200":
          description: Current dating location, or its configured absence.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProfileLocationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      operationId: replaceProfileLocation
      tags: [Profile]
      summary: Replace the current profile's private precise location
      description: Coordinates are accepted but never returned by this API.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProfileLocationUpdate"
      responses:
        "200":
          description: Private location state without coordinates.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProfileLocationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "422":
          $ref: "#/components/responses/ValidationFailed"
    delete:
      operationId: deleteProfileLocation
      tags: [Profile]
      summary: Soft-delete the current profile's precise location
      responses:
        "200":
          description: Location is no longer configured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProfileLocationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
  /api/v1/places:
    get:
      operationId: listPlaces
      tags: [Profile]
      summary: List the current brand's canonical place catalog
      description: |
        Brand/country-scoped hierarchy (currently region → city → locality for
        DateZA; `country_code = ZA` is implicit and never listed). Omit
        `parent_id` for the top-level regions; pass a region/city `id` as
        `parent_id` to list its children. Returns `404 place_not_found` if
        `parent_id` does not resolve within the brand's configured countries.
        `404 capability_not_configured` for brands without place selection
        (e.g. HookUs).
      parameters:
        - name: parent_id
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Selectable places at the requested level.
          content:
            application/json:
              schema:
                type: object
                required: [places]
                properties:
                  places:
                    type: array
                    items:
                      $ref: "#/components/schemas/Place"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/locations/search:
    get:
      operationId: searchLocations
      tags: [Profile]
      summary: Search area-level dating locations (suburb, city, town) for "Enter my area or suburb"
      description: |
        Server-side search over D8N's own geography data, scoped to the
        request brand's configured countries (DateZA: South Africa only) —
        the client never calls an external geocoding provider directly, and
        the provider behind this endpoint may change without changing this
        contract. Never returns a country-level entry, individual addresses,
        or raw coordinates: only area-level results (region, city, or
        locality) suitable as a general dating-area choice. Each result's
        `place_id` is directly usable as-is with
        `PUT /api/v1/profile/place` — there is no separate selection/token
        endpoint. Same brand gate as `GET /places`: `404
        capability_not_configured` for brands without place selection
        configured (e.g. HookUs).
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
            minLength: 2
          description: Free-text area/suburb/city query. Whitespace is normalized; at least 2 meaningful characters are required.
      responses:
        "200":
          description: Up to 8 normalized, area-level matches, best first.
          content:
            application/json:
              schema:
                type: object
                required: [results]
                properties:
                  results:
                    type: array
                    items:
                      $ref: "#/components/schemas/LocationSearchResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: The query was blank or below the minimum length.
          content:
            application/json:
              schema:
                type: object
                required: [error, min_length]
                properties:
                  error:
                    type: string
                    enum: [query_too_short]
                  min_length:
                    type: integer
        "429":
          $ref: "#/components/responses/RateLimited"
  /api/v1/profile/place:
    put:
      operationId: replaceProfilePlace
      tags: [Profile]
      summary: Set the current profile's dating location from the canonical place catalog
      description: |
        Resolves `place_id` (a region/city/locality, never a country) to its
        server-held centroid coordinates and writes the same authoritative
        location record `PUT /api/v1/profile/location` does — the two paths
        are mutually exclusive updates to one location, not parallel systems.
        A client cannot submit or influence coordinates directly; only a valid
        `place_id` within the brand's configured countries is accepted.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [place_id]
              properties:
                place_id:
                  type: integer
      responses:
        "200":
          description: Location configured from the selected place, without coordinates.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProfileLocationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Unknown place, or outside the brand's configured countries.
          content:
            application/json:
              schema:
                type: object
                required: [error]
                properties:
                  error:
                    type: string
                    enum: [invalid_place]
  /api/v1/profile/photos:
    get:
      operationId: listProfilePhotos
      tags: [Profile]
      summary: List the current profile's photos
      description: |
        Each attached image carries a short-lived signed retrieval `url` (direct
        from private R2) plus its `url_expires_in` seconds. Refetch to renew.
      responses:
        "200":
          description: Owner photo library.
          content:
            application/json:
              schema:
                type: object
                required: [photos]
                properties:
                  photos:
                    type: array
                    items:
                      $ref: "#/components/schemas/ProfilePhoto"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      operationId: createProfilePhoto
      tags: [Profile]
      summary: Attach a completed direct upload to the profile
      description: |
        Second step of the direct-to-R2 upload. Submit the `signed_id` returned by
        `POST /api/v1/profile/photos/uploads` after the bytes have been `PUT` to the
        presigned URL. D8N confirms the object exists in private R2, verifies its
        real file signature and size (the declared MIME is only a hint), and
        attaches it to the authenticated owner's profile. New photos begin
        `pending_review`; initial visibility follows the brand policy (HookUs and
        DateZA: `visible` immediately with asynchronous moderation; a brand
        without an explicit immediate policy: `hidden` until moderated). A
        `signed_id` may be attached only once. The upload is
        then processed asynchronously into a safe, EXIF-stripped display
        derivative (`processing_state`); other users only ever see that
        derivative, and only once it is `ready`. Until then the photo is not
        exposed in discovery/matches even if visible. The current brand's
        `configuration.collections[photos].maximum_count` is enforced atomically.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [signed_id]
              properties:
                signed_id:
                  type: string
                  description: Opaque signed id from the upload-intent response.
                position:
                  type: integer
                  minimum: 0
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "201":
          description: Photo accepted for processing/review.
          content:
            application/json:
              schema:
                type: object
                required: [photo]
                properties:
                  photo:
                    $ref: "#/components/schemas/ProfilePhoto"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Invalid/reused upload, invalid insertion position, or the configured photo maximum was reached.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/profile/photos/order:
    put:
      operationId: reorderProfilePhotos
      tags: [Profile]
      summary: Replace the current profile's complete kept-photo order
      description: |
        `photo_ids` must contain every and only kept photo id returned by the
        owner photo library, exactly once. The replacement is atomic and assigns
        sequential positions from zero. The first ordered owner photo is primary;
        public surfaces independently select the first deliverable ordered photo.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [photo_ids]
              properties:
                photo_ids:
                  type: array
                  uniqueItems: true
                  items:
                    type: integer
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "200":
          description: Complete owner photo library in its new order.
          content:
            application/json:
              schema:
                type: object
                required: [photos]
                properties:
                  photos:
                    type: array
                    items:
                      $ref: "#/components/schemas/ProfilePhoto"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Malformed, duplicate, incomplete, deleted, or foreign photo ids.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/profile/photos/uploads:
    post:
      operationId: createProfilePhotoUpload
      tags: [Profile]
      summary: Request a direct-to-R2 photo upload authorization
      description: |
        First step of the direct-to-R2 upload (control plane). Declare the file's
        `content_type`, `byte_size`, and base64 MD5 `checksum`. D8N authorizes the
        caller, validates the declared media, allocates the object identity, and
        returns a short-lived presigned `PUT` `url` plus the exact `headers` to send
        with it. Upload the bytes directly to that URL, then attach the returned
        `signed_id` via `POST /api/v1/profile/photos`. D8N chooses the object key;
        the client never receives R2 credentials.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content_type, byte_size, checksum]
              properties:
                content_type:
                  type: string
                  enum: [image/jpeg, image/png, image/webp]
                byte_size:
                  type: integer
                  minimum: 1
                  description: Exact byte size of the file to upload (max 10 MB).
                checksum:
                  type: string
                  description: Base64-encoded MD5 digest of the file bytes.
                filename:
                  type: string
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "201":
          description: Short-lived upload authorization.
          content:
            application/json:
              schema:
                type: object
                required: [upload]
                properties:
                  upload:
                    $ref: "#/components/schemas/ProfilePhotoUpload"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationFailed"
  /api/v1/profile/photos/{id}:
    delete:
      operationId: deleteProfilePhoto
      tags: [Profile]
      summary: Soft-delete a profile photo
      parameters:
        - $ref: "#/components/parameters/PhotoId"
      responses:
        "200":
          description: Soft-deleted owner photo record.
          content:
            application/json:
              schema:
                type: object
                required: [photo]
                properties:
                  photo:
                    $ref: "#/components/schemas/ProfilePhoto"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/discovery:
    get:
      operationId: getDiscovery
      tags: [Matching]
      summary: Get the brand's configured default Discovery surface
      description: |
        All configured surfaces apply the **same**
        eligibility rules (brand isolation, active/visible profiles,
        current-user exclusion, reciprocal age/gender/preference, distance,
        blocking/exclusions).

        For HookUs, `mode` selects a cursor feed:
          * `for_you` (default) — the compatibility-ranked feed.
          * `new_here` — the newest eligible profiles first, deterministic on
            recency; the compatibility payload is still included.
        `vibe`, `online`, `cursor`, and `limit` apply to these HookUs feeds.

        For DateZA, omitting `mode` resolves `discovery.curated_daily`. The first
        request in an Africa/Johannesburg calendar day finalizes a persisted,
        compatibility-ranked allocation of up to 10 profiles. Repeated requests
        return the same order and do not consume profiles or DateZA Find
        allowance. Safety and shared interaction exclusions are rechecked on
        delivery; invalid candidates are omitted without refill. The response has
        `next_cursor: null` and `selection` metadata. `count` is the number
        currently safe to return, so it may be below the original allocation.
        There is intentionally no `remaining` consumption counter.

        A HookUs `next_cursor` encodes the mode and every active facet, so a
        later page cannot switch strategy or silently drop/change a filter:
        replaying a cursor under a different mode/vibe/online yields
        `invalid_cursor`. An unknown mode yields `invalid_mode`.

        Each profile includes `photos`: safe, EXIF-stripped display derivatives
        with short-lived signed URLs. Raw originals are never exposed.
      parameters:
        - $ref: "#/components/parameters/DiscoveryMode"
        - $ref: "#/components/parameters/DiscoveryVibe"
        - $ref: "#/components/parameters/DiscoveryOnline"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "200":
          description: Configured strategy-ranked Discovery profiles and surface metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscoveryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Current profile is not active and discoverable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), or its contract does not enable Discovery
            (`matching_not_configured`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          $ref: "#/components/responses/InvalidInput"
  /api/v1/find:
    get:
      operationId: getFindProfiles
      tags: [Matching]
      summary: Explore DateZA-eligible profiles under the daily unique-exposure allowance
      description: |
        DateZA Find returns deterministic newest-first pages from the shared D8N
        eligibility scope. For a free member, at most 10 unique candidate profiles
        may be surfaced during one Africa/Johannesburg calendar day. Replaying a
        page or seeing a previously exposed candidate does not consume allowance
        again. Detail, Like, and Pass operations do not consume Find allowance.
        An otherwise eligible DateZA member may use Find while the identifier
        attached to the current login session is unverified; verification is
        required only when they proceed to an interaction surface.

        Every returned profile has `compatibility`: a deterministic DateZA v1
        score, confidence and bounded reason codes when at least 35% of weighted
        dimensions are comparable, otherwise null. Compatibility is computed
        after eligibility and exposure allocation and does not consume allowance.

        The cursor is opaque, signed, and bound to the resolved brand, current
        membership, DateZA Find policy version, and exact filter set. Filters only
        narrow the member's stored reciprocal age/gender/distance eligibility.
        DateZA Discovery is a separate, unimplemented product surface.
      parameters:
        - $ref: "#/components/parameters/FindCursor"
        - $ref: "#/components/parameters/FindLimit"
        - $ref: "#/components/parameters/FindMinAge"
        - $ref: "#/components/parameters/FindMaxAge"
        - $ref: "#/components/parameters/FindMaxDistance"
        - $ref: "#/components/parameters/FindRelationshipIntent"
      responses:
        "200":
          description: Eligible safe public profiles and authoritative daily allowance state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FindResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Current profile is not active and discoverable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), or its contract does not enable Find
            (`find_not_configured`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          $ref: "#/components/responses/InvalidInput"
        "429":
          $ref: "#/components/responses/RateLimited"
  /api/v1/profiles/{profile_id}:
    get:
      operationId: getPublicProfile
      tags: [Matching]
      summary: Get one member's safe public profile by its public UUID
      description: |
        Authoritative, refreshable detail for a single profile, resolved from the
        stable public UUID surfaced by discovery (`profiles[].id`). This is the
        source of truth for a profile page: a browser refresh, a new tab, or a
        deep link all resolve independently — no client-side cache is required
        for correctness.

        Requires an authenticated member with an active, discoverable profile.
        "Public" means *safe information visible to another eligible authenticated
        member* — it is NOT unauthenticated web-public data.

        The response extends the safe public profile shape used by discovery with
        prompts, categorized interests, explicit contact-verification state, and
        DateZA's existing pair compatibility payload where configured.
        Compatibility is presentation-only here and does not change eligibility
        or matching semantics. `photos` are safe, EXIF-stripped display derivatives
        with short-lived signed URLs, in deterministic position order; raw
        originals and storage keys are never exposed.

        Availability enforces the SAME fundamental safety rules as discovery —
        brand isolation, active/visible lifecycle, suspension, account closure,
        soft-deletion, and blocking in either direction — but deliberately does
        NOT reapply reciprocal age/gender/distance, so a profile reachable from
        discovery keeps resolving even after ranking or dating preferences change.
        Every unavailable case (unknown, cross-brand, hidden, inactive, suspended,
        closed, discarded, or blocked either way) returns the same neutral
        `profile_unavailable` (404) and never reveals why.
      parameters:
        - $ref: "#/components/parameters/PublicProfileId"
      responses:
        "200":
          description: The target member's safe public profile.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProfileDetailResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/InteractionForbidden"
        "404":
          description: |
            Target is unavailable. Unknown, cross-brand, hidden, inactive,
            suspended, closed, discarded, and blocked targets all share the
            neutral `profile_unavailable` response.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/likes/incoming:
    get:
      operationId: listIncomingLikes
      tags: [Matching]
      summary: List profiles who have liked the viewer
      description: |
        Active Likes where another profile has liked the authenticated viewer's
        profile, and the pair remains a current, actionable relationship state:
        not blocked in either direction, not yet a Match (a mutual Like
        surfaces through `GET /api/v1/matches` instead, not here), and
        otherwise visible under the same base policy every Matching surface
        shares. Newest-liked-first, cursor-paginated. Shares its authorization
        capability with `POST /api/v1/profiles/{profile_id}/likes`.
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Current incoming likes and an opaque next cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LikeListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/InteractionForbidden"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), or matching interactions are disabled by
            its contract (`matching_not_configured`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          $ref: "#/components/responses/InvalidInput"
  /api/v1/likes/outgoing:
    get:
      operationId: listOutgoingLikes
      tags: [Matching]
      summary: List profiles the viewer has liked
      description: |
        Active Likes created by the authenticated viewer that remain a
        current, actionable pre-match relationship state: not blocked in
        either direction, not yet a Match (a mutual Like surfaces through
        `GET /api/v1/matches` instead, not here), and otherwise visible under
        the same base policy every Matching surface shares. Newest-liked-first,
        cursor-paginated. Shares its authorization capability with
        `POST /api/v1/profiles/{profile_id}/likes`.
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Current outgoing likes and an opaque next cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LikeListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/InteractionForbidden"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), or matching interactions are disabled by
            its contract (`matching_not_configured`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          $ref: "#/components/responses/InvalidInput"
  /api/v1/profiles/{profile_id}/likes:
    post:
      operationId: likeProfile
      tags: [Matching]
      summary: Idempotently like an eligible public profile
      parameters:
        - $ref: "#/components/parameters/PublicProfileId"
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "200":
          description: Existing like returned idempotently.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LikeResponse"
        "201":
          description: Like created; a mutual like may also create a match.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LikeResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/InteractionForbidden"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), matching interactions are disabled by its
            contract (`matching_not_configured`), or the target profile is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          $ref: "#/components/responses/InteractionConflict"
  /api/v1/profiles/{profile_id}/pass:
    post:
      operationId: passProfile
      tags: [Matching]
      summary: Idempotently pass on an eligible public profile
      parameters:
        - $ref: "#/components/parameters/PublicProfileId"
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "200":
          description: Existing pass returned idempotently.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PassResponse"
        "201":
          description: Pass created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PassResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/InteractionForbidden"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), matching interactions are disabled by its
            contract (`matching_not_configured`), or the target profile is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          $ref: "#/components/responses/InteractionConflict"
  /api/v1/profiles/{profile_id}/hook:
    post:
      operationId: sendHook
      tags: [Hooks]
      summary: Send a 🔥 Hook (one opener) to an eligible profile
      description: |
        Creates the sender's single unsolicited opener to a recipient. The body is
        the FINAL text the sender chose — the default copy is a frontend concern
        and receiving it means nothing special. No Match/Conversation/Message is
        created yet: the opener is held on the Hook, and the sender is locked out
        from any further message until the recipient replies (there is literally no
        conversation to message through, and a second Hook to the same person is
        rejected).

        Reachability matches Like exactly: blocked (either direction), suspended,
        closed, hidden, cross-brand, self, and otherwise-ineligible targets all
        fail closed as `profile_unavailable` (404), never revealing which.

        V1 anti-spam: exactly one Hook per (sender → recipient) pair, ever — a
        second attempt (including after decline/expiry) returns `already_hooked`
        (409). Sending is also bounded by a per-sender daily allowance
        (`hook_rate_limited`, 429, with `Retry-After`).
      parameters:
        - $ref: "#/components/parameters/PublicProfileId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendHookRequest"
      responses:
        "201":
          description: The Hook was created and is pending the recipient's reply.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendHookResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), Hook is disabled by its contract
            (`hook_not_configured`), or the target profile is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: |
            Interaction conflict: `already_hooked` (already used your one Hook to
            this person), `already_matched`, `already_liked`, or `incoming_hook`
            (they Hooked you — reply from your inbox instead).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Blank (`message_blank`) or oversized (`message_too_long`) opener.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: The sender's daily Hook allowance is exhausted (`hook_rate_limited`).
          headers:
            Retry-After:
              description: Seconds until the sender may Hook again.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/hooks:
    get:
      operationId: listReceivedHooks
      tags: [Hooks]
      summary: List Hooks received and awaiting the current member's reply
      description: |
        The recipient inbox: live (pending, unexpired) Hooks addressed to the
        current member, newest-first and cursor-paginated. Each entry carries the
        opener text and the sender's safe public profile. Hooks are private — only
        the recipient sees them; a sender never sees their own sent Hooks here.
        Senders who became unavailable (blocked, suspended, closed) silently drop
        off without disclosure.
      parameters:
        - $ref: "#/components/parameters/HookLimit"
        - $ref: "#/components/parameters/Cursor"
      responses:
        "200":
          description: A page of live received Hooks.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HookInboxResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "404":
          $ref: "#/components/responses/HookNotConfigured"
        "422":
          description: Invalid `limit` (`invalid_limit`) or `cursor` (`invalid_cursor`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/hooks/{hook_id}/reply:
    post:
      operationId: replyToHook
      tags: [Hooks]
      summary: Reply to a Hook — accepts it and unlocks a conversation
      description: |
        The recipient's first reply. There is no separate accept step: replying IS
        acceptance. Atomically creates the canonical Match + Conversation,
        materializes the sender's opener as the first message and this text as the
        second, and returns the unlocked conversation. All further messages flow
        through the standard `POST /conversations/{conversation_id}/messages`.

        A Hook that is not live for this member — already accepted/declined,
        expired, blocked either way, or not addressed to the caller — returns the
        neutral `hook_unavailable` (404), never revealing which.
      parameters:
        - $ref: "#/components/parameters/HookId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HookReplyRequest"
      responses:
        "201":
          description: The Hook was accepted; the conversation is unlocked.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HookReplyResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), Hook is disabled by its contract
            (`hook_not_configured`), or the Hook is not live for this member
            (`hook_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Blank (`message_blank`) or oversized (`message_too_long`) reply.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/hooks/{hook_id}/decline:
    post:
      operationId: declineHook
      tags: [Hooks]
      summary: Decline a pending Hook
      description: |
        The recipient declines. Terminal and private: the sender is never told a
        decline happened (their `hook_state` simply collapses to `unavailable`,
        indistinguishable from being ignored-then-expired or blocked). A Hook that
        is not live for this member returns the neutral `hook_unavailable` (404).
      parameters:
        - $ref: "#/components/parameters/HookId"
      responses:
        "204":
          description: The Hook was declined; nothing is disclosed to the sender.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), Hook is disabled by its contract
            (`hook_not_configured`), or the Hook is not live for this member
            (`hook_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/profiles/{profile_id}/opener:
    post:
      operationId: sendOpener
      tags: [D8N Opener]
      summary: Send a D8N Opener (one curated opener) to an eligible profile
      description: |
        The generic, brand-configurable version of `POST
        /api/v1/profiles/{profile_id}/hook` — same engine (Hook model,
        Hooks::SendHook), different brand policy. A brand enabling `match.opener`
        with a curated catalog (DateZA) requires `opener_key` to reference an
        active entry from `GET /api/v1/profile/configuration` → `openers`; the
        resolved catalog text is what gets sent (there is no freeform field). No
        Match/Conversation/Message is created yet: the opener is held on the
        record, and the sender is locked out from any further message until the
        recipient replies (and a second Opener to the same person is rejected).

        Reachability matches Like exactly: blocked (either direction), suspended,
        closed, hidden, cross-brand, self, and otherwise-ineligible targets all
        fail closed as `profile_unavailable` (404), never revealing which.

        V1 anti-spam: exactly one Opener per (sender → recipient) pair, ever — a
        second attempt (including after decline/expiry) returns `already_hooked`
        (409). Sending is also bounded by a per-sender daily allowance
        (`hook_rate_limited`, 429, with `Retry-After`).
      parameters:
        - $ref: "#/components/parameters/PublicProfileId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendOpenerRequest"
      responses:
        "201":
          description: The Opener was created and is pending the recipient's reply.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendOpenerResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), D8N Opener is disabled by its contract
            (`opener_not_configured`), or the target profile is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: |
            Interaction conflict: `already_hooked` (already used your one Opener
            to this person), `already_matched`, `already_liked`, or
            `incoming_hook` (they sent you one — reply from your inbox instead).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Unknown or retired `opener_key` (`invalid_opener`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: The sender's daily Opener allowance is exhausted (`hook_rate_limited`).
          headers:
            Retry-After:
              description: Seconds until the sender may send an Opener again.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/openers:
    get:
      operationId: listReceivedOpeners
      tags: [D8N Opener]
      summary: List D8N Openers received and awaiting the current member's reply
      description: |
        The recipient inbox: live (pending, unexpired) Openers addressed to the
        current member, newest-first and cursor-paginated. Each entry carries the
        resolved opener text and the sender's safe public profile. Openers are
        private — only the recipient sees them; a sender never sees their own
        sent Openers here. Senders who became unavailable (blocked, suspended,
        closed) silently drop off without disclosure.
      parameters:
        - $ref: "#/components/parameters/OpenerLimit"
        - $ref: "#/components/parameters/Cursor"
      responses:
        "200":
          description: A page of live received D8N Openers.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenerInboxResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "404":
          $ref: "#/components/responses/OpenerNotConfigured"
        "422":
          description: Invalid `limit` (`invalid_limit`) or `cursor` (`invalid_cursor`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/openers/{opener_id}/reply:
    post:
      operationId: replyToOpener
      tags: [D8N Opener]
      summary: Reply to a D8N Opener — accepts it and unlocks a conversation
      description: |
        The recipient's first reply. There is no separate accept step: replying IS
        acceptance. Atomically creates the canonical Match + Conversation,
        materializes the sender's opener as the first message and this text as the
        second, and returns the unlocked conversation. All further messages flow
        through the standard `POST /conversations/{conversation_id}/messages`.

        An Opener that is not live for this member — already accepted/declined,
        expired, blocked either way, or not addressed to the caller — returns the
        neutral `hook_unavailable` (404), never revealing which.
      parameters:
        - $ref: "#/components/parameters/OpenerId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HookReplyRequest"
      responses:
        "201":
          description: The Opener was accepted; the conversation is unlocked.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HookReplyResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), D8N Opener is disabled by its contract
            (`opener_not_configured`), or the Opener is not live for this member
            (`hook_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Blank (`message_blank`) or oversized (`message_too_long`) reply.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/openers/{opener_id}/decline:
    post:
      operationId: declineOpener
      tags: [D8N Opener]
      summary: Decline a pending D8N Opener
      description: |
        The recipient declines. Terminal and private: the sender is never told a
        decline happened (their `opener_state` simply collapses to `unavailable`,
        indistinguishable from being ignored-then-expired or blocked). An Opener
        that is not live for this member returns the neutral `hook_unavailable`
        (404).
      parameters:
        - $ref: "#/components/parameters/OpenerId"
      responses:
        "204":
          description: The Opener was declined; nothing is disclosed to the sender.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), D8N Opener is disabled by its contract
            (`opener_not_configured`), or the Opener is not live for this member
            (`hook_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/hook_tonight:
    get:
      operationId: getHookTonight
      tags: [Hook Tonight]
      summary: Get the current member's Hook Tonight availability
      description: |
        Authoritative current availability, computed against the clock: a lapsed
        or deactivated activation reports `active: false`. Drives the CTA state
        ("🔥 Hook Tonight" when inactive, "🔥 You're available tonight" with the
        expiry when active).
      responses:
        "200":
          description: The current availability state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HookTonightState"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/HookTonightNotConfigured"
    post:
      operationId: activateHookTonight
      tags: [Hook Tonight]
      summary: Activate (or refresh) Hook Tonight availability
      description: |
        Turns Hook Tonight on for the current member and (re)stamps the expiry,
        surfacing them in the Hook Tonight discovery pool. Idempotent: re-posting
        while active simply refreshes the same single state row (no history is
        accumulated, safe under concurrent calls). Requires an active, discoverable
        member — a suspended, closed, or incomplete profile is rejected with
        `discoverable_profile_required`. A Hook Tonight activation grants NO extra
        Hook allowance; approaching someone still spends a normal 🔥 Hook.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HookTonightActivateRequest"
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "201":
          description: Hook Tonight is active.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HookTonightState"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: The member is not active and discoverable (`discoverable_profile_required`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          $ref: "#/components/responses/HookTonightNotConfigured"
        "422":
          description: The requested `intent` is not supported (`invalid_intent`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deactivateHookTonight
      tags: [Hook Tonight]
      summary: Deactivate Hook Tonight availability
      description: |
        Turns Hook Tonight off; the member immediately leaves the pool. The normal
        profile, existing Hooks, matches, and conversations are untouched.
        Idempotent — deactivating when already off is a safe no-op.
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "204":
          description: Hook Tonight is off (or was already off).
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`). Registered production brands may always
            clear Hook Tonight state even when the capability is disabled.
  /api/v1/hook_tonight/discovery:
    get:
      operationId: getHookTonightDiscovery
      tags: [Hook Tonight]
      summary: Get a page of members who are available tonight
      description: |
        The exact same eligible, ranked discovery population as `GET
        /api/v1/discovery` — identical brand isolation, active/visible lifecycle,
        blocking (either direction), reciprocal age/gender/preference, distance,
        exclusions (including live Hooks), facet filters, safe photos, and
        pagination — narrowed to members who currently have a live Hook Tonight
        availability. Each profile carries the same viewer-relative `hook_state`,
        so the client renders the correct 🔥 Hook action; approaching still goes
        through the existing Hook endpoints. This endpoint is permanently bound
        to the configured Hook Tonight restricted-pool surface; clients cannot
        redirect it to another Discovery mode. `vibe`, `online`, `cursor`, and
        `limit` use the shared configured Discovery machinery.

        Access is **reciprocal**: the viewer must have a live Hook Tonight
        activation to see the pool (to browse who's available tonight you must be
        available tonight). A viewer without one gets 403 `hook_tonight_required`;
        activate via `POST /api/v1/hook_tonight` first. Normal discovery
        (`GET /api/v1/discovery`) is unaffected and open to any eligible member.
      parameters:
        - $ref: "#/components/parameters/DiscoveryVibe"
        - $ref: "#/components/parameters/DiscoveryOnline"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "200":
          description: Available-tonight profiles and an opaque next cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscoveryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: |
            The viewer is not active and discoverable
            (`discoverable_profile_required`), or has no live Hook Tonight
            activation (`hook_tonight_required`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          $ref: "#/components/responses/HookTonightNotConfigured"
        "422":
          $ref: "#/components/responses/InvalidInput"
  /api/v1/blocks:
    get:
      operationId: listBlockedProfiles
      tags: [Trust]
      summary: List the profiles the current member has blocked
      description: |
        Returns only outgoing blocks (blocker is the current member), newest
        first, for the Settings / Safety "Blocked users" surface. Blocks whose
        target account was since removed are omitted. Each entry exposes the
        minimum profile information needed to recognise and unblock the target.
      responses:
        "200":
          description: The current member's blocked profiles.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlockedProfilesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
  /api/v1/profiles/{profile_id}/block:
    post:
      operationId: blockProfile
      tags: [Trust]
      summary: Idempotently block a current-brand profile
      description: |
        Blocking applies in either direction for discovery, interactions, match lists,
        and conversation access. It removes existing likes and ends an active match.
      parameters:
        - $ref: "#/components/parameters/PublicProfileId"
      responses:
        "200":
          description: The directional block already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlockResponse"
        "201":
          description: The directional block was created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlockResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: unblockProfile
      tags: [Trust]
      summary: Idempotently remove the current profile's directional block
      description: |
        Unknown, cross-brand, self, and soft-deleted targets receive the same no-content
        response. Unblocking does not restore likes or reactivate an ended match.
      parameters:
        - $ref: "#/components/parameters/PublicProfileId"
      responses:
        "204":
          description: The request completed without disclosing target or block state.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
  /api/v1/reports:
    post:
      operationId: fileReport
      tags: [Trust]
      summary: File a safety report against a profile or a specific piece of content
      description: |
        Content-level reporting foundation. The reporter is always the
        authenticated current-brand member (never the body); the target is
        identified by an opaque public id and the responsible profile is derived
        server-side from it. Supported targets: a `profile`, a chat `message`
        (reportable by a conversation participant, and still reportable after the
        sender is blocked/suspended/closed), a `profile_media` photo (reportable
        while the owner is visible to the viewer), a `hook` opener (reportable by
        its recipient), and a `conversation` (reportable by either participant,
        for pattern-level harm across several messages; works identically for a
        Match-origin or an accepted-Opener-origin conversation).

        Message evidence snapshots only the reported message. Conversation
        evidence snapshots a bounded window of the most recent messages that
        existed at report time — never the full history. Filing a report never
        blocks, unmatches, or hides the conversation; those are separate actions.

        Idempotent per open (reporter, target): a repeat returns `created: false`.
        Distinct content from the same person can each be reported. Unknown,
        inaccessible, and cross-brand targets all share the neutral
        `target_unavailable` (404) response so nothing is enumerable. The response
        reveals only that the report was received.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FileReportRequest"
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "200":
          description: An open report from this reporter against this target already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileReportResponse"
        "201":
          description: The report was filed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileReportResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "404":
          description: |
            The target is unknown, inaccessible to the reporter, or belongs to
            another brand — returned neutrally as `target_unavailable`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: |
            Malformed input — an unknown `reason` (`invalid_reason`) or unknown
            `target_type` (`invalid_target_type`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/profiles/{profile_id}/report:
    post:
      operationId: reportProfile
      tags: [Trust]
      summary: File a safety report against a current-brand profile
      description: |
        Reporting is independent of blocking and does not change the target's
        visibility, moderation status, or photo processing — it records an audit
        signal for administrative review. It is idempotent per open report: while
        an earlier report from the same reporter against the same target is still
        open, repeat submissions return `created: false` and do not stack.
        Unknown, cross-brand, and self targets share the `profile_unavailable`
        (404) response so target existence is never disclosed.
      parameters:
        - $ref: "#/components/parameters/PublicProfileId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReportRequest"
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "200":
          description: An open report from this reporter against this target already exists.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportResponse"
        "201":
          description: The report was filed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/ProfileOrMembershipRequired"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationFailed"
  /api/v1/matches:
    get:
      operationId: listMatches
      tags: [Matching]
      summary: List current live matches
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Live matches and an opaque next cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatchListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/InteractionForbidden"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), or matching is disabled by its contract
            (`matching_not_configured`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          $ref: "#/components/responses/InvalidInput"
  /api/v1/matches/{match_id}/unmatch:
    post:
      operationId: unmatchProfile
      tags: [Matching]
      summary: End an active Match without blocking the other member
      description: |
        Distinct from Trust's block endpoints: Unmatch ends the relationship
        (transitions the Match to its ended state and discards active Likes in
        both directions) but never creates a ProfileBlock — the pair remains
        free to encounter, like, and rematch each other later through normal
        discovery/Find/Likes surfaces. Block remains the separate, stronger
        safety action and is unaffected by this endpoint.

        Idempotent: unmatching an already-ended Match (whether ended by a
        prior Unmatch or by a Block) is a safe no-op and still returns 204.
        Only a current participant may unmatch; unknown, cross-brand, and
        non-participant match ids all receive the same `match_unavailable`
        response so nothing about the relationship is enumerable.

        The conversation and its message history are not deleted. New
        messages can no longer be sent or read through the ended Match (this
        is already enforced by the existing Messaging authorization that
        every conversation/message endpoint shares), but the conversation's
        card continues to appear in `GET /api/v1/conversations` as read-only
        history, exactly as it already does when a Match ends for any other
        reason.
      parameters:
        - $ref: "#/components/parameters/PublicMatchId"
      responses:
        "204":
          description: >-
            The Match is now ended (or already was). No Block was created.
            Likes between the pair were discarded.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: >-
            The authenticated profile is not currently a valid match member
            (matches `GET /api/v1/matches`'s handling of the same underlying
            check).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), unmatching is disabled by its contract
            (`matching_not_configured`), or the Match is unknown, cross-brand,
            or does not include the current profile (`match_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/conversations:
    get:
      operationId: listConversations
      tags: [Messaging]
      summary: List conversations for the current brand profile
      description: |
        Participant-scoped conversation cards, newest-first, each with a preview of
        its latest message (or null). Read the full history via the per-conversation
        messages endpoint.
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Participant-scoped conversations and an opaque next cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/InteractionForbidden"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), or messaging is disabled by its contract
            (`messaging_not_configured`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          $ref: "#/components/responses/InvalidInput"
  /api/v1/matches/{match_id}/conversation:
    post:
      operationId: startConversation
      tags: [Messaging]
      summary: Idempotently open the conversation for a live match
      parameters:
        - $ref: "#/components/parameters/PublicMatchId"
      responses:
        "200":
          description: Existing conversation returned idempotently.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartConversationResponse"
        "201":
          description: Conversation and both profile participants created atomically.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartConversationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/InteractionForbidden"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), messaging is disabled by its contract
            (`messaging_not_configured`), or the Match is unavailable,
            cross-brand, or does not include the current profile.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/conversations/{conversation_id}/messages:
    get:
      operationId: listMessages
      tags: [Messaging]
      summary: List a conversation's messages, newest first
      description: |
        Participant-scoped, cursor-paginated plain-text message history for one
        conversation, ordered newest-first by (created_at, id). Only the two
        conversation participants may read. Unknown, cross-brand, non-participant,
        ended, unavailable (suspended/deleted participant), and blocked
        conversations all return the same neutral 404 `conversation_unavailable`;
        the block is never disclosed. History is always bounded by `limit`; page
        backward into older messages with `next_cursor`. A message cursor is bound
        to its brand, viewer, and conversation and cannot be replayed elsewhere.
      parameters:
        - $ref: "#/components/parameters/PublicConversationId"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/MessageLimit"
      responses:
        "200":
          description: A page of messages, newest first, and an opaque next cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/InteractionForbidden"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), messaging is disabled by its contract
            (`messaging_not_configured`), or the Conversation is unavailable,
            cross-brand, ended, blocked, or not the current profile's.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          $ref: "#/components/responses/InvalidInput"
    post:
      operationId: sendMessage
      tags: [Messaging]
      summary: Send a plain-text message to a conversation
      description: |
        Appends one plain-text message after re-authorizing the sender
        (participant, active match, both participants available, neither side
        blocking the other). The body is treated as untrusted input, NFC-normalized,
        and must be non-blank and at most 2000 characters; Unicode/emoji is
        preserved. Content never enters logs. Beta accepts at-least-once client
        sends (no idempotency key), so a retried request creates a second message.
      parameters:
        - $ref: "#/components/parameters/PublicConversationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMessageRequest"
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "201":
          description: The message was persisted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendMessageResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/InteractionForbidden"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), messaging is disabled by its contract
            (`messaging_not_configured`), or the Conversation is unavailable,
            cross-brand, ended, blocked, or not the current profile's.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: >-
            Body and attachments are both absent (`message_blank`), body is too
            long (`message_too_long`), too many attachments
            (`too_many_attachments`), or an attachment reference is invalid
            (`invalid_media_kind`, `upload_already_used`, `upload_not_found`,
            `invalid_attachment`, `invalid_attachment_upload`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/conversations/{conversation_id}/attachments/uploads:
    post:
      operationId: createMessageAttachmentUpload
      tags: [Messaging]
      summary: Request a direct-to-R2 upload authorization for a chat image or video (D8N Chat Media)
      description: |
        Control-plane step of the direct-to-R2 upload for a chat attachment.
        Authorizes the caller through the same participant/active-match/
        availability/block gate as sending a message itself. Declare
        `media_kind`, `content_type`, `byte_size`, and a base64 MD5 `checksum`;
        D8N allocates the object identity and returns a short-lived presigned
        `PUT` `url` plus the exact `headers` to send with it. Upload the bytes
        directly to that URL, then reference the returned `signed_id` in
        `attachment_uploads` on `POST /conversations/{conversation_id}/messages`
        — attach and send are the same operation, there is no separate finalize
        endpoint. The client never receives R2 credentials or chooses the
        object key. A video's `byte_size_limit` is a technical abuse ceiling
        (storage/bandwidth/worker protection), not a product-facing video
        length/size limit.
      parameters:
        - $ref: "#/components/parameters/PublicConversationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateMessageAttachmentUploadRequest"
      responses:
        "429":
          $ref: "#/components/responses/RateLimited"
        "201":
          description: Short-lived upload authorization.
          content:
            application/json:
              schema:
                type: object
                required: [upload]
                properties:
                  upload:
                    $ref: "#/components/schemas/MessageAttachmentUpload"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/InteractionForbidden"
        "404":
          description: >-
            The resolved database brand has no production contract
            (`brand_not_configured`), D8N Chat Media is disabled by its contract
            (`messaging_not_configured`), or the conversation is unavailable,
            cross-brand, ended, blocked, or not the current profile's.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: >-
            Missing/invalid upload parameters (`upload_parameters_required`),
            unknown `media_kind` (`invalid_media_kind`), unsupported
            `content_type` for that media kind (`unsupported_content_type`),
            `byte_size` beyond the technical ceiling (`invalid_byte_size`), or
            a missing checksum (`checksum_required`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/conversations/{conversation_id}/messages/{message_id}/attachments/{id}:
    delete:
      operationId: deleteMessageAttachment
      tags: [Messaging]
      summary: Delete an attachment the caller sent, for everyone (D8N Chat Media)
      description: |
        "Delete for everyone" at attachment granularity — only the specific
        attachment is removed; the message and any other attachments/body are
        unaffected. Only the message's sender may delete; a recipient cannot
        remove another profile's media. Once deleted, no further `view_url`,
        `download_url`, or `poster_url` is issued for this attachment (it is
        shown with `deleted: true`), and the underlying R2 object(s) are purged
        as soon as Active Storage's purge queue runs. This is deliberately
        NOT reachable once the conversation itself is unavailable (unmatch,
        block, suspension) — attachment access always follows the same
        participant/active-match gate as the rest of messaging, so there is no
        path to fetch or delete an attachment after messaging access ends.
      parameters:
        - $ref: "#/components/parameters/PublicConversationId"
        - $ref: "#/components/parameters/PublicMessageId"
        - $ref: "#/components/parameters/PublicAttachmentId"
      responses:
        "200":
          description: The attachment was deleted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteMessageAttachmentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/InteractionForbidden"
        "404":
          description: >-
            The conversation, message, or attachment is unavailable
            (`conversation_unavailable`, `message_unavailable`,
            `attachment_unavailable`), or the caller did not send it
            (`attachment_not_owned`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/notifications:
    get:
      operationId: listNotifications
      tags: [Notifications]
      summary: List the current member's brand-scoped product notifications
      description: |
        Returns at most 50 newest-first notifications for the authenticated user
        and active membership in the host-resolved brand. The same D8N identity's
        notifications in another brand are never returned. Presentation-safe copy
        is derived from the stable notification type; provider, recipient, device,
        delivery-attempt, and internal database fields are never exposed.
      responses:
        "200":
          description: The current brand inbox and unread count.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/notifications/read_all:
    post:
      operationId: markAllNotificationsRead
      tags: [Notifications]
      summary: Mark every current-brand notification read
      responses:
        "200":
          description: Number of notifications newly marked read.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MarkAllNotificationsReadResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/notifications/{id}/read:
    patch:
      operationId: markNotificationRead
      tags: [Notifications]
      summary: Mark one current-brand notification read
      description: Unknown and cross-brand notification ids return the same neutral 404.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The notification with its read timestamp.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Notification is unknown or belongs to another brand/member.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/notifications/preferences:
    get:
      operationId: showNotificationPreferences
      tags: [Notifications]
      summary: Effective notification delivery preferences for the current brand membership
      description: |
        Returns the authenticated member's current DateZA/HookUs product
        notification delivery preferences, scoped to their brand membership in
        the host-resolved brand — the same D8N identity's preferences in
        another brand are entirely independent rows. If no preference row has
        been saved yet (or the current brand membership is not active), this
        returns the same effective defaults `Notifications::Policy` already
        applies when deciding delivery — both fields default to `true` — never
        an error, and reading never creates a row.

        These two fields only control the *delivery channel* for D8N product
        notifications (in-app is never affected — see
        `GET /api/v1/notifications`). They do not control whether an
        underlying dating event (e.g. `match_created`) exists.
      responses:
        "200":
          description: Effective current preferences (persisted or default).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationPreferencesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      operationId: updateNotificationPreferences
      tags: [Notifications]
      summary: Partially update notification delivery preferences
      description: |
        Updates only the fields provided; omitted fields are left unchanged.
        Both fields must be real JSON booleans — a string like `"true"` is
        rejected, not coerced. Unknown keys are rejected rather than silently
        accepted, since a UI toggle must correspond to real backend behaviour
        (there is currently no generic product SMS or WhatsApp delivery to
        control — see the T4/T5 implementation reports).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NotificationPreferencesUpdate"
      responses:
        "200":
          description: The preferences after applying the update.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationPreferencesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: The current brand membership does not exist (no membership to attach preferences to).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: An unknown preference key was supplied, or a value was not a JSON boolean.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/admin/profile_photos:
    get:
      operationId: listAdminProfilePhotoQueue
      tags: [Trust]
      summary: List the profile photo moderation review queue for the current brand
      description: |
        Requires verified admin MFA and `admin.profile_photos.moderate`.
        Returns every kept, still-undecided
        (pending_review) profile photo for the request's brand, oldest-first
        by (created_at, id), bounded to 100 entries. This is the discovery
        path a moderator needs to find photos awaiting a decision before
        calling PATCH /api/v1/admin/profile_photos/{id} — required for any
        brand configured moderate-first (see the DateZA media policy).
        Approved and rejected photos never appear here. Cross-brand photos are
        simply absent, never disclosed.
      responses:
        "200":
          description: The current moderation queue.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminProfilePhotoQueueResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
  /api/v1/admin/profile_photos/{id}:
    patch:
      operationId: moderateProfilePhoto
      tags: [Trust]
      summary: Apply a terminal moderation decision to a pending profile photo
      description: |
        Requires verified admin MFA and `admin.profile_photos.moderate` on the
        request brand.
        The opaque photo UUID is resolved only inside that brand. Approval makes
        the placement visible; rejection makes it hidden and independently
        non-deliverable. Repeating the same terminal decision is idempotent;
        attempting the opposite terminal decision returns a conflict. Every real
        transition is audited without media bytes, URLs, keys, or free-form notes.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [approved, rejected]
      responses:
        "200":
          description: Current terminal state and whether this request performed the transition.
          content:
            application/json:
              schema:
                type: object
                required: [photo, transitioned]
                properties:
                  transitioned:
                    type: boolean
                  photo:
                    $ref: "#/components/schemas/AdminProfilePhotoModeration"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The photo already has the opposite terminal decision.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          $ref: "#/components/responses/ValidationFailed"
  /api/v1/admin/reports:
    get:
      operationId: listReports
      tags: [Admin]
      summary: List the moderation queue for the current brand
      description: |
        Requires verified admin MFA and `admin.reports.read`. Returns reports for the request's brand
        (resolved from the host — a brand id is never accepted from the client),
        oldest-first by (created_at, id) so the longest-waiting reports surface
        first, cursor-paginated and bounded. Access requires an active AdminUser
        with an active assignment for this brand: unauthenticated callers get 401,
        ordinary users and admins assigned elsewhere get 403. Cross-brand reports
        are simply absent, never disclosed.
      parameters:
        - $ref: "#/components/parameters/ReportStatusFilter"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/AdminReportLimit"
      responses:
        "200":
          description: A page of reports and an opaque next cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminReportListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "422":
          $ref: "#/components/responses/InvalidInput"
  /api/v1/admin/reports/{id}:
    get:
      operationId: getReport
      tags: [Admin]
      summary: Inspect one report
      description: |
        Requires verified admin MFA and `admin.reports.read`. The read is audited (a sensitive-read
        SecurityEvent is recorded). Unknown and cross-brand ids share the neutral
        `report_unavailable` (404). Only moderator-relevant fields are returned;
        no credentials, sessions, email/phone, private location, or media.
      parameters:
        - $ref: "#/components/parameters/ReportId"
      responses:
        "200":
          description: The report.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminReportResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "404":
          description: Report is unknown or belongs to another brand.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      operationId: updateReport
      tags: [Admin]
      summary: Apply a moderation lifecycle transition
      description: |
        Requires verified admin MFA and `admin.reports.moderate`. Applies one lifecycle transition and records the
        decision (audited; `reviewed_by`/`reviewed_at`/`resolution_note` set). Only
        `status` and an optional short `note` are accepted; report internals
        (reporter, target, brand, reason, timestamps) can never be mass-assigned.
        Transitions: `open → reviewing | dismissed`, `reviewing → actioned |
        dismissed | open`. `actioned`/`dismissed` are terminal. `actioned` means
        "resolved as warranting action" — it does NOT suspend or ban the account;
        account enforcement is a separate capability (TS-04). A disallowed target
        from a non-terminal state is `invalid_transition` (422); a transition out of
        a terminal state (e.g. another moderator already resolved it) is
        `report_conflict` (409).
      parameters:
        - $ref: "#/components/parameters/ReportId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateReportRequest"
      responses:
        "200":
          description: The updated report.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminReportResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "404":
          description: Report is unknown or belongs to another brand.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The report is already in a terminal state (`report_conflict`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Invalid `status`, `invalid_transition`, or an oversized `note`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/admin/profiles/{profile_id}/suspension:
    post:
      operationId: suspendProfile
      tags: [Admin]
      summary: Apply a brand-level suspension to a profile's owner
      description: |
        Requires verified admin MFA and `admin.enforcements.manage`. Brand-level enforcement: suspends the target's
        membership in this brand, revokes all of their sessions for this brand, and
        records an audited, reversible `AccountEnforcement`. Everything happens in
        one transaction. The target's network identity and any other-brand access
        are untouched (platform-wide bans are not modelled yet). Enforcement is
        deliberately separate from the report lifecycle: an optional `report_id`
        links the cause without changing that report's status. Idempotent-by-
        conflict: a second suspension of an already-suspended user returns 409
        `already_suspended`. Cross-brand/unknown targets return neutral
        `profile_unavailable` (404).
      parameters:
        - $ref: "#/components/parameters/PublicProfileId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SuspendProfileRequest"
      responses:
        "201":
          description: The suspension was applied.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminEnforcementResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "404":
          description: Target is unknown or belongs to another brand (`profile_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The target is already suspended (`already_suspended`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: An unknown `report_id` (`invalid_report`) or an oversized `reason`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: reinstateProfile
      tags: [Admin]
      summary: Lift a brand-level suspension
      description: |
        Requires verified admin MFA and `admin.enforcements.manage`. Reactivates the membership and marks the
        enforcement reverted, audited, in one transaction. Restores ONLY what
        suspension changed — it does not revive revoked sessions (the user logs in
        again), ended matches, blocks, or deleted data. Reinstating a profile that
        is not suspended returns 409 `not_suspended`.
      parameters:
        - $ref: "#/components/parameters/PublicProfileId"
      responses:
        "200":
          description: The suspension was lifted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminEnforcementResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "404":
          description: Target is unknown or belongs to another brand (`profile_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The target is not currently suspended (`not_suspended`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/hq/operator:
    get:
      operationId: getCurrentHqOperator
      tags: [Hq]
      summary: Resolve the current operator, capabilities, assignments, and MFA state
      description: |
        Available after ordinary authentication and a current-brand admin
        assignment; MFA step-up is not required for this bootstrap response.
        `effective_capabilities`, never the role label, is authoritative for UI
        affordances. Other brand assignments are informational switcher targets;
        each brand still requires its own brand-bound session and authorization.
      responses:
        "200":
          description: Current administrative identity and session security state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqCurrentOperatorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
  /api/v1/hq/operators:
    get:
      operationId: listHqOperators
      tags: [Hq]
      summary: List at most 100 operator assignments for the current brand
      responses:
        "200":
          description: Brand-scoped operator assignments.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqOperatorListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
    post:
      operationId: createHqOperatorAssignment
      tags: [Hq]
      summary: Assign an existing current-brand member as an operator
      description: |
        Requires `admin.operators.manage` and verified MFA. The target must
        already have an active membership on this brand. Founder is
        bootstrap-only; only Founder may grant Super Admin. Self-assignment and
        grants outside the actor's authority are rejected and audited.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HqCreateOperatorRequest"
      responses:
        "201":
          description: Operator assignment created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqOperatorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/InvalidInput"
  /api/v1/hq/operators/{id}:
    patch:
      operationId: updateHqOperatorAssignment
      tags: [Hq]
      summary: Change or revoke a current-brand operator assignment
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HqUpdateOperatorRequest"
      responses:
        "200":
          description: Updated current-brand assignment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqOperatorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/InvalidInput"
  /api/v1/hq/mfa/enrollment:
    post:
      operationId: startHqMfaEnrollment
      tags: [Hq]
      summary: Start TOTP enrollment for the current admin identity
      description: |
        Ordinary authenticated admin session required; prior MFA is not. The
        returned secret/provisioning URI is enrollment-only sensitive data and
        must never be logged or persisted by the frontend.
      responses:
        "201":
          description: Pending TOTP enrollment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqMfaEnrollmentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "422":
          $ref: "#/components/responses/InvalidInput"
    patch:
      operationId: confirmHqMfaEnrollment
      tags: [Hq]
      summary: Confirm enrollment and return one-time recovery codes
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HqMfaCodeRequest"
      responses:
        "200":
          description: MFA active; recovery codes are returned once only.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqMfaConfirmationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "422":
          $ref: "#/components/responses/InvalidInput"
        "429":
          $ref: "#/components/responses/AdminMfaRateLimited"
    delete:
      operationId: resetHqMfaEnrollment
      tags: [Hq]
      summary: Reset MFA after verified-session and fresh-factor proof
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HqMfaCodeRequest"
      responses:
        "200":
          description: MFA disabled; every session requires enrollment again.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqMfaStateResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "422":
          $ref: "#/components/responses/InvalidInput"
        "429":
          $ref: "#/components/responses/AdminMfaRateLimited"
  /api/v1/hq/mfa/challenge:
    post:
      operationId: challengeHqMfa
      tags: [Hq]
      summary: Step up the current session with TOTP or one-time recovery code
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HqMfaCodeRequest"
      responses:
        "200":
          description: Current session is MFA verified.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqMfaChallengeResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "422":
          $ref: "#/components/responses/InvalidInput"
        "429":
          $ref: "#/components/responses/AdminMfaRateLimited"
  /api/v1/hq/analytics/overview:
    get:
      operationId: getHqAnalyticsOverview
      tags: [Hq]
      summary: Current-brand growth and activity overview
      description: |
        Requires verified admin MFA and `hq.analytics.read`; brand-scoped and read-only. Returns
        signup and active-member rollups, gender split, and total registered members for the
        host-resolved brand. Week boundaries are Sunday through Saturday; month boundaries start on
        the first day of the calendar month, both in the brand timezone. The read is audited as
        `hq.analytics_overview_viewed`.
      responses:
        "200":
          description: Current analytics overview for the host-resolved brand.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqAnalyticsOverviewResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
  /api/v1/hq/trust_safety/overview:
    get:
      operationId: getHqTrustSafetyOverview
      tags: [Hq]
      summary: Current-brand Trust & Safety queue and enforcement overview
      description: |
        Requires verified admin MFA and `hq.trust_safety.read`; brand-scoped and read-only. Returns current
        report counts by status, reason, and target type; open-queue aging; and
        enforcement totals. `overdue` is deliberately null and `sla_status` is
        `not_configured` because D8N has not defined an SLA threshold. The read
        is audited as `hq.trust_safety_overview_viewed` without recording report
        content, filters, or member PII.
      responses:
        "200":
          description: Current Trust & Safety overview for the host-resolved brand.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqTrustSafetyOverviewResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
  /api/v1/hq/trust_safety/repeat_offenders:
    get:
      operationId: listHqTrustSafetyRepeatOffenders
      tags: [Hq]
      summary: List repeatedly reported profiles for the current brand
      description: |
        Requires verified admin MFA and `hq.trust_safety.read`; brand-scoped and read-only. "Repeat" means
        at least two retained reports received by one profile. Results are
        ordered by report count descending, then latest report descending, and
        bounded rather than cursor-paginated. This is an investigation signal,
        never an automatic enforcement decision. `member_360_lookup` can be
        passed to GET /api/v1/hq/members/{lookup}; it is null when the retained
        historical profile is no longer available to Member 360. The read is
        audited as `hq.trust_safety_repeat_offenders_viewed`.
      parameters:
        - $ref: "#/components/parameters/HqRepeatOffenderLimit"
      responses:
        "200":
          description: Bounded repeat-offender aggregation for the current brand.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqRepeatOffenderListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "422":
          description: Invalid `limit` (`invalid_limit`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/hq/trust_safety/enforcements:
    get:
      operationId: listHqTrustSafetyEnforcements
      tags: [Hq]
      summary: Paginated brand-wide account-enforcement history
      description: |
        Requires verified admin MFA and `hq.trust_safety.read`; brand-scoped, read-only, and newest-first.
        Optional `state` selects active or reverted enforcement. The opaque
        cursor is signed and bound to both the host-resolved brand and the state
        filter. The response reuses the established AdminEnforcement shape, so
        `profile_id` is the Member 360 lookup/deep-link when present. The read is
        audited as `hq.trust_safety_enforcements_viewed`; only the non-sensitive
        state filter is retained in audit metadata.
      parameters:
        - $ref: "#/components/parameters/HqEnforcementStateFilter"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/HqHistoryLimit"
      responses:
        "200":
          description: A page of brand-wide enforcement history and an opaque next cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqEnforcementListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "422":
          description: Invalid `state`, `limit`, or `cursor` (`invalid_filter` / `invalid_limit` / `invalid_cursor`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/hq/members:
    get:
      operationId: listHqMembers
      tags: [Hq]
      summary: List members for the current brand
      description: |
        Requires verified admin MFA and `hq.member.sensitive_read`. Results are
        limited to kept memberships on the host-resolved brand and are ordered
        newest-first by membership creation time. Safe operational summaries only;
        contact identifiers and Member 360 detail remain behind the member lookup
        endpoint. Reads are audited as `hq.member_directory_viewed`.
      parameters:
        - $ref: "#/components/parameters/HqMemberDirectoryStatus"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/HqMemberDirectoryLimit"
      responses:
        "200":
          description: A page of member directory rows and an opaque next cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqMemberDirectoryListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "422":
          description: Invalid `status`, `limit`, or `cursor` (`invalid_filter` / `invalid_limit` / `invalid_cursor`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/hq/members/{lookup}:
    get:
      operationId: getMember360
      tags: [Hq]
      summary: Look up a member and return their Member 360 operational summary
      description: |
        Requires verified admin MFA and `hq.member.sensitive_read`. `lookup`
        may be an email, a phone number (in
        whatever form `POST /api/v1/auth/password/login` would accept), or a
        profile `public_id` UUID. Resolution is always scoped to the request's
        brand: an identifier that exists only on another brand, or does not
        exist at all, returns the same neutral `member_unavailable` (404) — no
        enumeration. The read is audited (a `hq.member_360_viewed`
        SecurityEvent is recorded). Six thin, bounded sections are returned:
        identity, profile, product, comms, safety, activity. Full paginated
        history for security events, auth attempts, and enforcements is NOT
        included here — see the three sub-resources below.
      parameters:
        - $ref: "#/components/parameters/HqLookup"
      responses:
        "200":
          description: The member's identity summary and six-section Member 360 payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqMember360Response"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "404":
          description: No member on this brand matches `lookup` (`member_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/hq/members/{lookup}/security_events:
    get:
      operationId: listMemberSecurityEvents
      tags: [Hq]
      summary: Paginated SecurityEvent history for one member
      description: |
        Requires verified admin MFA and `hq.member.security_read`; brand-scoped. Newest-first, cursor-paginated
        (the cursor is bound to this brand, this member, and this resource
        type — it cannot be replayed elsewhere). The read is audited
        (`hq.member_security_events_viewed`). This is the same `SecurityEvent`
        table used throughout the product's audit trail (report views,
        enforcement actions, this HQ read itself).
      parameters:
        - $ref: "#/components/parameters/HqLookup"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/HqHistoryLimit"
      responses:
        "200":
          description: A page of security events and an opaque next cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqSecurityEventListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "404":
          description: No member on this brand matches `lookup` (`member_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Invalid `limit` or `cursor` (`invalid_limit` / `invalid_cursor`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/hq/members/{lookup}/auth_attempts:
    get:
      operationId: listMemberAuthAttempts
      tags: [Hq]
      summary: Paginated AuthAttempt history for one member
      description: |
        Requires verified admin MFA and `hq.member.security_read`; brand-scoped. Newest-first, cursor-paginated,
        identical pagination semantics to `security_events`. The read is
        audited (`hq.member_auth_attempts_viewed`). Closes the previously
        console-only read gap over `AuthAttempt` (login/OTP/recovery attempts,
        including throttled and locked outcomes) — see
        docs/FOUNDER-HQ/D8N-HQ/CURRENT-STATE.md #1.4.
      parameters:
        - $ref: "#/components/parameters/HqLookup"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/HqHistoryLimit"
      responses:
        "200":
          description: A page of auth attempts and an opaque next cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqAuthAttemptListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "404":
          description: No member on this brand matches `lookup` (`member_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Invalid `limit` or `cursor` (`invalid_limit` / `invalid_cursor`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/hq/members/{lookup}/enforcements:
    get:
      operationId: listMemberEnforcements
      tags: [Hq]
      summary: Paginated AccountEnforcement history for one member
      description: |
        Requires verified admin MFA and `hq.member.security_read`; brand-scoped. Newest-first, cursor-paginated,
        identical pagination semantics to `security_events`. The read is
        audited (`hq.member_enforcements_viewed`). Returns the same
        `AdminEnforcement` shape as `POST /api/v1/admin/profiles/{profile_id}/suspension`
        — this is the full history that endpoint only ever showed one row of.
      parameters:
        - $ref: "#/components/parameters/HqLookup"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/HqHistoryLimit"
      responses:
        "200":
          description: A page of enforcements and an opaque next cursor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqEnforcementListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "404":
          description: No member on this brand matches `lookup` (`member_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Invalid `limit` or `cursor` (`invalid_limit` / `invalid_cursor`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/hq/members/{lookup}/discovery_diagnostic:
    get:
      operationId: getMemberDiscoveryDiagnostic
      tags: [Hq]
      summary: "\"Why is Discover empty for this member?\" read-only funnel diagnostic"
      description: |
        Requires verified admin MFA and `hq.discovery_diagnostics.read`;
        brand-scoped. Requires the member to have a
        profile (`profile_unavailable` 404 otherwise). Re-runs the live
        discovery engine's own eligibility stages (Matching::VisibilityScope,
        Matching::EligibilityScope, Matching::ExclusionsScope) read-only,
        reporting a candidate count at each stage — it never mutates
        `DiscoveryAllocation`/`DiscoveryAllocationCandidate` and never consumes
        the member's normal daily discovery quota. When the member themselves
        is not currently eligible to see discovery at all (incomplete profile,
        suspended, etc.), `eligible: false` is returned with a reason and no
        stages, instead of an error. The read is audited
        (`hq.member_discovery_diagnostic_viewed`).
      parameters:
        - $ref: "#/components/parameters/HqLookup"
      responses:
        "200":
          description: Eligibility and, if eligible, a stage-by-stage candidate-count breakdown.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HqDiscoveryDiagnosticResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AdminForbidden"
        "404":
          description: No member matches `lookup`, or the member has no profile yet (`member_unavailable` / `profile_unavailable`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: opaque
      description: Opaque brand-bound D8N session token.
    cookieAuth:
      type: apiKey
      in: cookie
      name: d8n_web_session
      description: Host-only HttpOnly brand-bound browser session credential.
  parameters:
    DiscoveryMode:
      name: mode
      in: query
      required: false
      description: |
        Discovery feed selector. Omitted or `for_you` returns the default
        compatibility-ranked feed; `new_here` returns the newest eligible
        profiles first. Eligibility is identical across modes. An unsupported
        value returns 422 `invalid_mode`.
      schema:
        type: string
        enum: [for_you, new_here]
        default: for_you
    DiscoveryVibe:
      name: vibe
      in: query
      required: false
      description: |
        Optional facet filter: a public "vibes" option code (e.g.
        `420_friendly`). Narrows the feed to profiles that selected it, on top of
        the normal eligibility rules, and combines with any `mode`. An
        unsupported value returns 422 `invalid_filter`.
      schema:
        type: string
    DiscoveryOnline:
      name: online
      in: query
      required: false
      description: |
        Optional "online now" facet. When `true`, narrows the feed to profiles
        whose user has been active in the last 10 minutes. Combines with `mode`
        and `vibe`.
      schema:
        type: boolean
    Cursor:
      name: cursor
      in: query
      required: false
      description: Opaque signed cursor returned by the same endpoint and brand.
      schema:
        type: string
    Limit:
      name: limit
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 50
        default: 20
    FindCursor:
      name: cursor
      in: query
      required: false
      description: Opaque signed cursor bound to the brand, membership, policy, and filters.
      schema:
        type: string
    FindLimit:
      name: limit
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 10
        default: 10
    FindMinAge:
      name: min_age
      in: query
      required: false
      schema:
        type: integer
        minimum: 18
        maximum: 120
    FindMaxAge:
      name: max_age
      in: query
      required: false
      schema:
        type: integer
        minimum: 18
        maximum: 120
    FindMaxDistance:
      name: max_distance_km
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 500
    FindRelationshipIntent:
      name: relationship_intent
      in: query
      required: false
      description: Active DateZA relationship-intent option code; narrows eligible candidates.
      schema:
        type: string
    PublicProfileId:
      name: profile_id
      in: path
      required: true
      description: Stable public profile UUID from discovery.
      schema:
        type: string
        format: uuid
    PhotoId:
      name: id
      in: path
      required: true
      description: Owner photo record ID.
      schema:
        type: integer
    PublicMatchId:
      name: match_id
      in: path
      required: true
      description: Stable public match UUID from the match list.
      schema:
        type: string
        format: uuid
    PublicConversationId:
      name: conversation_id
      in: path
      required: true
      description: Stable public conversation UUID from the conversation list or start-conversation response.
      schema:
        type: string
        format: uuid
    PublicMessageId:
      name: message_id
      in: path
      required: true
      description: Stable public message UUID from a message list/send response.
      schema:
        type: string
        format: uuid
    PublicAttachmentId:
      name: id
      in: path
      required: true
      description: Stable public message-attachment UUID from a message's `attachments` array.
      schema:
        type: string
        format: uuid
    MessageLimit:
      name: limit
      in: query
      required: false
      description: Page size for message history.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 30
    HookId:
      name: hook_id
      in: path
      required: true
      description: Stable public Hook UUID from the received-Hooks inbox.
      schema:
        type: string
        format: uuid
    HookLimit:
      name: limit
      in: query
      required: false
      description: Page size for the received-Hooks inbox.
      schema:
        type: integer
        minimum: 1
        maximum: 50
        default: 20
    OpenerId:
      name: opener_id
      in: path
      required: true
      description: Stable public D8N Opener UUID from the received-openers inbox.
      schema:
        type: string
        format: uuid
    OpenerLimit:
      name: limit
      in: query
      required: false
      description: Page size for the received-openers inbox.
      schema:
        type: integer
        minimum: 1
        maximum: 50
        default: 20
    ReportId:
      name: id
      in: path
      required: true
      description: Internal report id (admin-only; scoped to the moderator's brand).
      schema:
        type: integer
    ReportStatusFilter:
      name: status
      in: query
      required: false
      description: Narrow the queue to one lifecycle status. Unknown values return 422 `invalid_filter`.
      schema:
        type: string
        enum: [open, reviewing, actioned, dismissed]
    AdminReportLimit:
      name: limit
      in: query
      required: false
      description: Page size for the moderation queue.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
    HqLookup:
      name: lookup
      in: path
      required: true
      description: An email, a phone number, or a profile `public_id` UUID. Resolved only within the request's brand.
      schema:
        type: string
    HqHistoryLimit:
      name: limit
      in: query
      required: false
      description: Page size for an HQ member-history sub-resource.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
    HqRepeatOffenderLimit:
      name: limit
      in: query
      required: false
      description: "Maximum repeat-offender rows returned. The response reports `truncated: true` when more rows exist."
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
    HqEnforcementStateFilter:
      name: state
      in: query
      required: false
      description: Restrict brand-wide enforcement history to active or reverted rows.
      schema:
        type: string
        enum: [active, reverted]
    HqMemberDirectoryStatus:
      name: status
      in: query
      required: false
      description: Restrict the member directory to one membership status.
      schema:
        type: string
        enum: [active, suspended, left, deactivated]
    HqMemberDirectoryLimit:
      name: limit
      in: query
      required: false
      description: Page size for the brand member directory.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
  responses:
    Unauthorized:
      description: Missing, invalid, expired, revoked, or wrong-brand session. Cookie bootstrap may return `session_expired` or `session_revoked`; other cases return `unauthorized`.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    CsrfRejected:
      description: Cookie-authenticated unsafe request omitted or supplied an invalid `X-CSRF-Token` (`csrf_token_invalid`).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InteractionForbidden:
      description: >-
        For a published DateZA member, the identifier attached to the current
        login session has not been verified (`identifier_verification_required`).
        Existing profile, membership, and lifecycle authorization errors remain
        distinct. This DateZA-only rule does not change HookUs authorization.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    HookNotConfigured:
      description: >-
        The resolved database brand has no production contract
        (`brand_not_configured`), or Hook is disabled by its contract
        (`hook_not_configured`).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    OpenerNotConfigured:
      description: >-
        The resolved database brand has no production contract
        (`brand_not_configured`), or D8N Opener is disabled by its contract
        (`opener_not_configured`).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    HookTonightNotConfigured:
      description: >-
        The resolved database brand has no production contract
        (`brand_not_configured`), or Hook Tonight is disabled by its contract
        (`hook_tonight_not_configured`).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    AdminForbidden:
      description: |
        Administrative access denied. `forbidden` means there is no active
        current-brand assignment or required effective capability (including
        revoked access). `admin_mfa_required` means the assignment/capability is
        valid but this ordinary session has not completed backend MFA step-up.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    AdminMfaRateLimited:
      description: Too many recent admin MFA failures (`admin_mfa_rate_limited`).
      headers:
        Retry-After:
          schema:
            type: integer
          description: Seconds before another MFA proof may be attempted.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    BrandRequired:
      description: Request host did not resolve to an active brand.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    AuthMethodUnavailable:
      description: Request host did not resolve to an active brand, or this authentication method is disabled.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    RateLimited:
      description: >-
        Rate limit exceeded (`rate_limited`). Returned by authentication throttles
        and by the generic abuse-protection layer on write/read surfaces
        (messaging, likes, passes, reports, media uploads, profile writes,
        discovery, Hook Tonight). The 🔥 Hook send endpoint has its own distinct
        product limit (`hook_rate_limited`) and is documented separately.
      headers:
        Retry-After:
          schema:
            type: integer
          description: Seconds before another request should be attempted.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    ProfileOrMembershipRequired:
      description: Required brand membership/profile/lifecycle state is unavailable.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InvalidInput:
      description: Request input, cursor, or limit is invalid.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    ValidationFailed:
      description: Validation failed.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ValidationError"
    ProfileValidationFailed:
      description: >-
        Profile values failed validation (`invalid_profile`), or the request
        submitted known D8N profile fields that the resolved brand has not
        enabled (`invalid_profile_fields`).
      content:
        application/json:
          schema:
            oneOf:
              - $ref: "#/components/schemas/ValidationError"
              - $ref: "#/components/schemas/InvalidProfileFields"
    NotFound:
      description: Resource is unavailable, including cross-brand resources.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InteractionConflict:
      description: Interaction conflicts with the current participant state.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
  schemas:
    HealthResponse:
      type: object
      required: [status, app, api_version, checks]
      properties:
        status:
          type: string
          enum: [ok, degraded]
        app:
          const: d8n
        api_version:
          const: v1
        checks:
          type: object
          required: [primary_database, queue_database]
          properties:
            primary_database:
              type: string
              enum: [ok, unavailable]
            queue_database:
              type: string
              enum: [ok, unavailable]
    VersionResponse:
      type: object
      required:
        [app, git_sha, release, image_version, environment,
         rails_environment, build_timestamp, booted_at]
      properties:
        app:
          const: d8n
        git_sha:
          type: [string, "null"]
          pattern: "^[0-9a-fA-F]{40}$"
        release:
          type: [string, "null"]
          description: Kamal image version when present, otherwise the immutable git SHA.
        image_version:
          type: [string, "null"]
          description: Runtime `KAMAL_VERSION`; may be a deployment alias, so use `git_sha` for source identity.
        environment:
          type: string
          description: D8N deployment environment such as staging or production.
        rails_environment:
          type: string
        build_timestamp:
          type: [string, "null"]
          format: date-time
        booted_at:
          type: string
          format: date-time
    HqOperatorRole:
      type: string
      enum:
        [founder, super_admin, operations, trust_safety, support,
         engineering, marketing, analyst, moderator]
    HqCapability:
      type: string
      enum:
        - hq.member.sensitive_read
        - hq.member.security_read
        - hq.discovery_diagnostics.read
        - hq.trust_safety.read
        - admin.reports.read
        - admin.reports.moderate
        - admin.enforcements.manage
        - admin.profile_photos.moderate
        - admin.operators.read
        - admin.operators.manage
        - admin.brand_operations.manage
        - hq.system.read
        - hq.analytics.read
    HqOperatorAssignment:
      type: object
      required: [brand, role, effective_capabilities]
      properties:
        brand:
          type: string
        role:
          $ref: "#/components/schemas/HqOperatorRole"
        effective_capabilities:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/HqCapability"
    HqMfaState:
      type: object
      required: [state, required, verified, recovery_codes_remaining]
      properties:
        state:
          type: string
          enum: [not_enrolled, pending, active]
        required:
          const: true
        verified:
          type: boolean
          description: True only when this exact session completed step-up against the current active credential.
        recovery_codes_remaining:
          type: [integer, "null"]
          minimum: 0
    HqCurrentOperatorResponse:
      type: object
      required: [operator]
      properties:
        operator:
          type: object
          required:
            [admin_user_id, user_id, status, current_brand, role,
             effective_capabilities, grantable_roles, brand_assignments, mfa]
          properties:
            admin_user_id:
              type: integer
            user_id:
              type: integer
            status:
              type: string
              enum: [active, suspended, disabled]
            current_brand:
              type: string
            role:
              $ref: "#/components/schemas/HqOperatorRole"
            effective_capabilities:
              type: array
              uniqueItems: true
              items:
                $ref: "#/components/schemas/HqCapability"
            grantable_roles:
              type: array
              uniqueItems: true
              description: Server-authoritative roles this actor may grant; Founder itself is never returned.
              items:
                $ref: "#/components/schemas/HqOperatorRole"
            brand_assignments:
              type: array
              items:
                $ref: "#/components/schemas/HqOperatorAssignment"
            mfa:
              $ref: "#/components/schemas/HqMfaState"
    HqOperator:
      type: object
      required:
        [admin_user_id, user_id, admin_status, assignment_status, role,
         effective_capabilities, mfa_enrolled]
      properties:
        admin_user_id:
          type: integer
        user_id:
          type: integer
        admin_status:
          type: string
          enum: [active, suspended, disabled]
        assignment_status:
          type: string
          enum: [active, suspended, revoked]
        role:
          $ref: "#/components/schemas/HqOperatorRole"
        effective_capabilities:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/HqCapability"
        mfa_enrolled:
          type: boolean
    HqOperatorResponse:
      type: object
      required: [operator]
      properties:
        operator:
          $ref: "#/components/schemas/HqOperator"
    HqOperatorListResponse:
      type: object
      required: [operators]
      properties:
        operators:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/HqOperator"
    HqCreateOperatorRequest:
      type: object
      required: [email, role]
      additionalProperties: false
      properties:
        email:
          type: string
          format: email
          description: Existing verified D8N identity with an active membership on the current brand.
        role:
          $ref: "#/components/schemas/HqOperatorRole"
    HqUpdateOperatorRequest:
      type: object
      minProperties: 1
      additionalProperties: false
      properties:
        role:
          $ref: "#/components/schemas/HqOperatorRole"
        status:
          type: string
          enum: [active, suspended, revoked]
    HqMfaCodeRequest:
      type: object
      required: [code]
      additionalProperties: false
      properties:
        code:
          type: string
          description: Six-digit TOTP or one-time recovery code. Never logged or audited.
    HqMfaEnrollmentResponse:
      type: object
      required: [mfa]
      properties:
        mfa:
          type: object
          required: [state, secret, provisioning_uri]
          properties:
            state:
              const: pending
            secret:
              type: string
              description: Enrollment-only Base32 secret; display once and never persist in frontend storage/logs.
            provisioning_uri:
              type: string
              format: uri
    HqMfaConfirmationResponse:
      type: object
      required: [mfa, recovery_codes]
      properties:
        mfa:
          type: object
          required: [state, verified]
          properties:
            state:
              const: active
            verified:
              const: true
        recovery_codes:
          type: array
          minItems: 8
          maxItems: 8
          description: Returned once; prompt the operator to store these offline.
          items:
            type: string
    HqMfaChallengeResponse:
      type: object
      required: [mfa]
      properties:
        mfa:
          type: object
          required: [state, verified, method, recovery_codes_remaining]
          properties:
            state:
              const: active
            verified:
              const: true
            method:
              type: string
              enum: [totp, recovery_code]
            recovery_codes_remaining:
              type: integer
              minimum: 0
    HqMfaStateResponse:
      type: object
      required: [mfa]
      properties:
        mfa:
          type: object
          required: [state, verified]
          properties:
            state:
              const: not_enrolled
            verified:
              const: false
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: string
    ValidationError:
      allOf:
        - $ref: "#/components/schemas/ErrorResponse"
        - type: object
          required: [details]
          properties:
            details:
              type: object
              additionalProperties:
                type: array
                items:
                  type: string
    InvalidProfileFields:
      allOf:
        - $ref: "#/components/schemas/ErrorResponse"
        - type: object
          required: [details]
          properties:
            error:
              const: invalid_profile_fields
            details:
              type: object
              required: [fields]
              properties:
                fields:
                  type: array
                  items:
                    type: string
    MessageResponse:
      type: object
      required: [message]
      properties:
        message:
          type: string
    BrandSummary:
      type: object
      required: [slug, name]
      properties:
        slug:
          type: string
        name:
          type: string
    AuthMethodsResponse:
      type: object
      required: [brand, methods]
      properties:
        brand:
          $ref: "#/components/schemas/BrandSummary"
        methods:
          type: array
          uniqueItems: true
          items:
            type: string
            enum: [phone_password, email_password, google]
    IdentifierVerificationRequest:
      type: object
      required: [kind]
      properties:
        kind:
          type: string
          enum: [phone, email]
    VerificationDeliveryResponse:
      type: object
      required: [message, resend_available_in, expires_at]
      properties:
        message:
          type: string
        resend_available_in:
          type: integer
          minimum: 0
          description: Server-owned seconds until another code request is eligible.
        expires_at:
          type: [string, "null"]
          format: date-time
          description: Server-authoritative contact-verification expiry; null for the generic no-delivery response.
    SessionIdentifierVerification:
      type: object
      required: [kind, verified, masked_destination]
      properties:
        kind:
          type: string
          enum: [phone, email]
        verified:
          type: boolean
        masked_destination:
          type: string
          description: Safe display form of the current session credential identifier; never the raw value.
    VerificationDispatchState:
      type: object
      required: [code_dispatched, resend_available_in, expires_at]
      properties:
        code_dispatched:
          type: boolean
          description: True only while a usable verification challenge is queued or confirmed sent.
        resend_available_in:
          type: integer
          minimum: 0
          description: Server-owned seconds until another code request is eligible.
        expires_at:
          type: [string, "null"]
          format: date-time
          description: Expiry of the usable contact-verification code (one hour after issue), or null when none was dispatched.
    PasswordAuthRequest:
      type: object
      required: [identifier, password]
      properties:
        identifier:
          type: string
          description: International phone number or email address; normalized by the server.
        password:
          type: string
          format: password
          minLength: 6
          description: At least 6 characters and no more than 72 UTF-8 bytes.
        device_name:
          type: [string, "null"]
          maxLength: 120
        session_mode:
          type: string
          enum: [browser]
          description: Omit for bearer mode. Use `browser` for an HttpOnly cookie without a bearer secret in JSON.
    PasswordChangeRequest:
      type: object
      required: [current_password, password, password_confirmation]
      properties:
        current_password:
          type: string
          format: password
        password:
          type: string
          format: password
          minLength: 6
          description: At least 6 characters, no more than 72 UTF-8 bytes, and different from the current password.
        password_confirmation:
          type: string
          format: password
    EmailChangeRequest:
      type: object
      required: [email, current_password]
      properties:
        email:
          type: string
          format: email
          maxLength: 254
          description: Proposed replacement login email. It is not activated until code confirmation.
        current_password:
          type: string
          format: password
          description: Current password for the email-backed credential used by this session.
    EmailChangeConfirmation:
      type: object
      required: [email, code]
      properties:
        email:
          type: string
          format: email
          maxLength: 254
          description: The same proposed email supplied to the request step.
        code:
          type: string
          pattern: "^[0-9]{6}$"
          description: Single-use code delivered to the proposed email; expires after 10 minutes.
    EmailChangeResponse:
      type: object
      required: [identifier, revoked_session_count]
      properties:
        identifier:
          type: object
          required: [kind, verified]
          properties:
            kind:
              const: email
            verified:
              const: true
        revoked_session_count:
          type: integer
          minimum: 0
          description: Other active sessions revoked because the login identifier changed.
    PasswordRecoveryRequest:
      type: object
      required: [identifier]
      properties:
        identifier:
          type: string
          description: International phone number or email address; normalized by the server.
    PasswordRecoveryVerifyRequest:
      type: object
      required: [identifier, code]
      properties:
        identifier:
          type: string
          description: The same identifier used to request recovery.
        code:
          type: string
          pattern: "^[0-9]{6}$"
          description: The recovery code delivered to the identifier's channel.
    PasswordResetAuthorization:
      type: object
      required: [reset_token, expires_at]
      properties:
        reset_token:
          type: string
          description: Single-use secret authorizing one password reset. Send it only to the reset endpoint.
        expires_at:
          type: string
          format: date-time
          description: When the reset authorization expires (15 minutes after issue).
    PasswordResetRequest:
      type: object
      required: [reset_token, password, password_confirmation]
      properties:
        reset_token:
          type: string
          description: The reset authorization token returned by the verify step.
        password:
          type: string
          format: password
          minLength: 6
          description: At least 6 characters and no more than 72 UTF-8 bytes.
        password_confirmation:
          type: string
          format: password
    IdentifierVerification:
      type: object
      required: [kind, code]
      properties:
        kind:
          type: string
          enum: [phone, email]
        code:
          type: string
          pattern: "^[0-9]{6}$"
    IdentifierVerificationResponse:
      type: object
      required: [identifier]
      properties:
        identifier:
          type: object
          required: [kind, verified]
          properties:
            kind:
              type: string
              enum: [phone, email]
            verified:
              const: true
    AuthSessionResponse:
      type: object
      required: [token, token_type, expires_at, user_id, brand]
      properties:
        token:
          type: string
          description: Secret bearer token returned only when the session is issued.
        token_type:
          const: Bearer
        expires_at:
          type: string
          format: date-time
        user_id:
          type: integer
          description: Private platform identity ID; do not use as a public dating-profile ID.
        brand:
          $ref: "#/components/schemas/BrandSummary"
    PasswordAuthSessionResponse:
      type: object
      required: [expires_at, user_id, brand, identifier, verification_required, verification_channel, verification, onboarding]
      oneOf:
        - required: [token, token_type]
        - required: [browser_session]
      properties:
        token:
          type: string
          description: Present only in default bearer mode; never present in browser mode.
        token_type:
          const: Bearer
        browser_session:
          type: object
          required: [persisted, csrf_token]
          properties:
            persisted:
              const: true
            csrf_token:
              type: string
              description: Send as `X-CSRF-Token` on unsafe cookie-authenticated requests.
        expires_at:
          type: string
          format: date-time
        user_id:
          type: integer
        brand:
          $ref: "#/components/schemas/BrandSummary"
        identifier:
          $ref: "#/components/schemas/SessionIdentifierVerification"
        verification_required:
          type: boolean
        verification_channel:
          type: [string, "null"]
          enum: [phone, email, null]
        verification:
          $ref: "#/components/schemas/VerificationDispatchState"
        onboarding:
          $ref: "#/components/schemas/ProfileOnboardingStatus"
    MeResponse:
      type: object
      required: [user_id, brand, session, identifier, verification_required, verification, account_status, account_controls]
      properties:
        user_id:
          type: integer
        brand:
          $ref: "#/components/schemas/BrandSummary"
        session:
          type: object
          required: [id, expires_at, authentication_mode, csrf_token]
          properties:
            id:
              type: integer
            expires_at:
              type: string
              format: date-time
            authentication_mode:
              type: string
              enum: [bearer, cookie]
            csrf_token:
              type: [string, "null"]
              description: Present for cookie authentication; null for bearer authentication.
        identifier:
          oneOf:
            - $ref: "#/components/schemas/SessionIdentifierVerification"
            - type: "null"
        verification_required:
          type: boolean
        verification:
          oneOf:
            - $ref: "#/components/schemas/VerificationDispatchState"
            - type: "null"
        account_status:
          const: active
          description: |
            Always "active" here — an authenticated session can only exist against
            an active BrandMembership (see SessionAuthenticator), so a deactivated
            or deleted account can never reach this endpoint. A deactivated
            account is observed by the client as an `account_deactivated` error
            from `POST /api/v1/auth/password/login`; a deleted account is
            observed as simply being logged out. There is no `deletion_pending`
            state because account closure is immediate.
        account_controls:
          $ref: "#/components/schemas/AccountControls"
    AccountControls:
      type: object
      required: [password_change, deactivation, deletion]
      description: Brand-policy availability of D8N ID account-lifecycle capabilities, for Settings to render only supported actions.
      properties:
        password_change:
          type: boolean
        deactivation:
          type: boolean
        deletion:
          type: boolean
    AccountDeactivationRequest:
      type: object
      required: [confirmation]
      properties:
        confirmation:
          type: string
          enum: [deactivate]
          description: Must equal "deactivate" to authorize this action.
    AccountDeactivationResponse:
      type: object
      required: [deactivated, already_deactivated]
      properties:
        deactivated:
          const: true
        already_deactivated:
          type: boolean
          description: True when the account was already deactivated (idempotent).
    CloseAccountRequest:
      type: object
      required: [confirmation]
      properties:
        confirmation:
          type: string
          enum: [close]
          description: Must equal "close" to authorize this one-way destructive action.
    CloseAccountResponse:
      type: object
      required: [closed, already_closed, media_purge_state]
      properties:
        closed:
          const: true
        already_closed:
          type: boolean
          description: True when the account was already closed (idempotent).
        media_purge_state:
          type: string
          enum: [pending, completed, failed]
          description: Async physical media-purge status; starts pending.
    Completion:
      type: object
      description: |
        Publication completeness: whether the member may publish and enter the
        dating experience. A required photo must have a ready safe derivative
        and be admitted by brand policy; rejected/deleted/failed/unready photos
        never count. Moderate-first brands may count a hidden pending safe
        derivative for onboarding while still withholding it publicly. Distinct
        from RichProfileCompletion.
      required: [complete, percent, missing]
      properties:
        complete:
          type: boolean
        percent:
          type: integer
          minimum: 0
          maximum: 100
        missing:
          type: array
          items:
            type: string
        sections:
          type: object
          description: |
            Informational per-section completeness (photos, bio, basics, intent,
            lifestyle, interests, prompts, verification). Present on the owner
            profile only. Purely a client nudge — never affects requirements,
            publication, or visibility.
          additionalProperties:
            type: object
            required: [complete]
            properties:
              complete:
                type: boolean
    RichProfileCompletion:
      type: [object, "null"]
      description: |
        Optional post-onboarding richness score. null means the brand has not
        composed a richness model. It never affects publication or eligibility.
      required: [percent, level, missing, suggestions, sections]
      properties:
        percent:
          type: integer
          minimum: 0
          maximum: 100
        level:
          type: string
          enum: [starter, growing, good, standout]
        missing:
          type: array
          items:
            type: string
            enum: [more_photos, bio, looking_for, interests, prompt, work_or_education, lifestyle, relationship_intent, family_plans, languages, personality]
        suggestions:
          type: array
          items:
            type: object
            required: [key, label]
            properties:
              key:
                type: string
              label:
                type: string
        sections:
          type: object
          additionalProperties:
            type: object
            required: [percent, complete]
            properties:
              percent:
                type: integer
                minimum: 0
                maximum: 100
              complete:
                type: boolean
    ProfileCounts:
      type: object
      description: |
        Backward-compatible stored/kept-record counts. These are not silently
        redefined as public/renderable counts; use the returned active prompt and
        interest arrays when that distinction matters.
      required: [photos, prompts, interests]
      properties:
        photos:
          type: integer
          minimum: 0
          description: Kept owner photo placements, regardless of processing/moderation delivery state.
        prompts:
          type: integer
          minimum: 0
        interests:
          type: integer
          minimum: 0
    ContactVerification:
      type: object
      required: [contact]
      description: |
        Contact-control verification only: at least one kept email or phone
        identifier has been verified. No RealMe or identity proof is implied.
      properties:
        contact:
          type: object
          required: [verified]
          properties:
            verified:
              type: boolean
    PublicationState:
      type: object
      required: [published, status, visibility]
      properties:
        published:
          type: boolean
        status:
          type: string
          enum: [draft, active, suspended]
        visibility:
          type: string
          enum: [hidden, visible]
    ProfileUpdate:
      type: object
      description: |
        Owner-scoped onboarding update. `first_name` and `last_name` update the
        private platform identity and are never part of public profile JSON.
        `display_name` remains the separate, brand-owned public dating identity.
        Only fields advertised by the resolved brand's
        `GET /api/v1/profile/configuration` contract may be submitted. Known D8N
        fields disabled for that brand return `invalid_profile_fields` (422).
      properties:
        first_name:
          type: [string, "null"]
          maxLength: 100
          description: Private, self-declared account identity. Required by DateZA completion; not a verified claim.
        last_name:
          type: [string, "null"]
          maxLength: 100
          description: Private, self-declared account identity. Required by DateZA completion and never publicly exposed.
        display_name:
          type: [string, "null"]
          maxLength: 80
        bio:
          type: [string, "null"]
          maxLength: 1000
        birthdate:
          type: [string, "null"]
          format: date
        gender:
          type: [string, "null"]
          maxLength: 40
        pronouns:
          type: [string, "null"]
          maxLength: 40
        visibility:
          type: string
          enum: [hidden, visible]
        country_code:
          type: [string, "null"]
          pattern: "^[A-Z]{2}$"
        city:
          type: [string, "null"]
          maxLength: 120
        occupation:
          type: [string, "null"]
          maxLength: 120
        job_title:
          type: [string, "null"]
          maxLength: 120
        company_name:
          type: [string, "null"]
          maxLength: 120
          description: Employer. Optional and NEVER exposed to other members (owner-only).
        school_or_institution:
          type: [string, "null"]
          maxLength: 160
        looking_for_text:
          type: [string, "null"]
          maxLength: 600
        children_count:
          type: [integer, "null"]
          minimum: 0
          maximum: 30
          description: Optional; owner-only. No other child detail is stored or exposed.
        height_cm:
          type: [integer, "null"]
          minimum: 100
          maximum: 250
        body_type:
          type: [string, "null"]
          maxLength: 80
        languages_spoken:
          type: array
          maxItems: 15
          description: Legacy free-text languages, retained for backward compatibility. Prefer `languages`.
          items:
            type: string
            maxLength: 40
        languages:
          type: array
          maxItems: 15
          description: |
            Canonical structured languages. `code` must be a supported taxonomy
            code; `proficiency` and `primary` are optional (at most one primary).
          items:
            $ref: "#/components/schemas/Language"
        smoking:
          type: [string, "null"]
          enum: [never, occasionally, regularly, null]
        drinking:
          type: [string, "null"]
          enum: [never, occasionally, regularly, null]
        fitness:
          type: [string, "null"]
          enum: [never, occasionally, regularly, null]
    OwnerProfile:
      allOf:
        - $ref: "#/components/schemas/ProfileUpdate"
        - type: object
          description: >-
            Owner profile scalars are present only when enabled by the resolved
            brand configuration. Stable platform envelope fields remain present.
          required: [id, brand, status, visibility, location, options, prompts, counts, verification, publication, completion, publication_completion, profile_completion]
          properties:
            id:
              type: string
              format: uuid
            brand:
              $ref: "#/components/schemas/BrandSummary"
            status:
              type: string
              enum: [draft, active, suspended]
            visibility:
              type: string
              enum: [hidden, visible]
            location:
              $ref: "#/components/schemas/OwnerProfileLocation"
            options:
              $ref: "#/components/schemas/ProfileOptionSelections"
            prompts:
              type: array
              items:
                $ref: "#/components/schemas/PromptAnswer"
            completion:
              $ref: "#/components/schemas/Completion"
            publication_completion:
              $ref: "#/components/schemas/Completion"
            profile_completion:
              $ref: "#/components/schemas/RichProfileCompletion"
            counts:
              $ref: "#/components/schemas/ProfileCounts"
            verification:
              $ref: "#/components/schemas/ContactVerification"
            publication:
              $ref: "#/components/schemas/PublicationState"
    OwnerProfileResponse:
      type: object
      required: [profile]
      properties:
        profile:
          $ref: "#/components/schemas/OwnerProfile"
    CurrentProfileResponse:
      type: object
      required: [profile, onboarding]
      properties:
        profile:
          oneOf:
            - $ref: "#/components/schemas/OwnerProfile"
            - type: "null"
        onboarding:
          $ref: "#/components/schemas/ProfileOnboardingStatus"
    ProfileOnboardingStatus:
      type: object
      required: [state, next_step, profile_exists, profile_complete, profile_published, completion]
      properties:
        state:
          type: string
          enum: [profile_required, profile_incomplete, ready_to_publish, complete, profile_suspended]
        next_step:
          type: [string, "null"]
          enum: [profile, preferences, photos, location, options, publication, null]
        profile_exists:
          type: boolean
        profile_complete:
          type: boolean
        profile_published:
          type: boolean
        completion:
          $ref: "#/components/schemas/Completion"
    ProfileOptionSelections:
      type: object
      additionalProperties:
        type: array
        items:
          type: string
    Language:
      type: object
      required: [code]
      description: |
        A structured language entry. On write, only `code` is required (`proficiency`
        and `primary` optional; at most one primary). On read, `label` is included.
      properties:
        code:
          type: string
          description: Supported taxonomy code (ISO 639-1), e.g. "en".
        label:
          type: string
          readOnly: true
          description: Human-readable label from the taxonomy (read-only).
        proficiency:
          type: [string, "null"]
          enum: [native, fluent, conversational, learning, null]
        primary:
          type: boolean
    ProfileLocation:
      type: object
      required: [precision]
      description: |
        Safe, approximate location metadata for presentation — never raw
        coordinates. Viewer-relative `distance_km` is supplied separately in status.
      properties:
        city:
          type: [string, "null"]
        country_code:
          type: [string, "null"]
        precision:
          type: string
          enum: [approximate]
    OwnerProfileLocation:
      type: object
      required: [configured]
      description: Owner-safe location state. Raw coordinates are never returned.
      properties:
        configured:
          type: boolean
        place:
          type: object
          required: [name, display_path]
          properties:
            name:
              type: string
            display_path:
              type: string
    ProfileInterest:
      type: object
      required: [slug, label]
      properties:
        slug:
          type: string
        label:
          type: string
        category:
          type: [string, "null"]
    PromptAnswer:
      type: object
      required: [key, prompt, answer, position]
      properties:
        key:
          type: string
        prompt:
          type: string
          description: The prompt text (copy) at read time.
        answer:
          type: string
        position:
          type: integer
          minimum: 0
    PromptAnswerInput:
      type: object
      required: [key, answer]
      properties:
        key:
          type: string
        answer:
          type: string
          maxLength: 300
    PublicProfilePhoto:
      type: object
      required: [id, position, primary, url, url_expires_in]
      description: |
        A safe, D8N-generated display derivative of a profile photo. It is
        decoded, re-encoded, and stripped of EXIF/GPS/other metadata. Only
        deliverable photos (kept, visible, moderation-eligible, and finished
        processing) appear; rejected media never appears even if its visibility
        is inconsistent. The raw
        original and its storage key are never exposed. The `url` is a
        short-lived signed URL — refetch the profile to renew it.
      properties:
        id:
          type: string
          format: uuid
          description: |
            Stable, opaque photo id. This is the `target_id` used to report this
            specific photo via POST /api/v1/reports; never the internal id or the
            R2 object key.
        position:
          type: integer
          minimum: 0
        primary:
          type: boolean
          description: True only for the first deliverable photo in deterministic display order.
        url:
          type: string
          format: uri
        url_expires_in:
          type: integer
    PublicProfile:
      type: object
      description: >-
        Public scalar and derived fields are present only when their source field
        is enabled by the profile's brand. Stable platform envelope fields remain
        present.
      required: [id, photos, options]
      properties:
        id:
          type: string
          format: uuid
        display_name:
          type: [string, "null"]
        age:
          type: [integer, "null"]
          minimum: 18
        bio:
          type: [string, "null"]
        gender:
          type: [string, "null"]
        pronouns:
          type: [string, "null"]
        country_code:
          type: [string, "null"]
        city:
          type: [string, "null"]
        location:
          oneOf:
            - $ref: "#/components/schemas/ProfileLocation"
            - type: "null"
        occupation:
          type: [string, "null"]
        job_title:
          type: [string, "null"]
        school_or_institution:
          type: [string, "null"]
        looking_for_text:
          type: [string, "null"]
        height_cm:
          type: [integer, "null"]
        body_type:
          type: [string, "null"]
        languages_spoken:
          type: array
          description: Legacy free-text languages, retained for backward compatibility.
          items:
            type: string
        languages:
          type: array
          description: Canonical structured languages (with taxonomy label).
          items:
            $ref: "#/components/schemas/Language"
        smoking:
          type: [string, "null"]
        drinking:
          type: [string, "null"]
        fitness:
          type: [string, "null"]
        photos:
          type: array
          description: |
            Safe display derivatives, ordered by position. The first returned item
            is the public primary photo. Empty when no photo is deliverable.
          items:
            $ref: "#/components/schemas/PublicProfilePhoto"
        options:
          description: |
            Selections for public_profile-visibility groups. On the profile-detail
            endpoint this ALSO includes matches_only groups when an active Match
            exists. company_name and owner-only groups are never included.
          $ref: "#/components/schemas/ProfileOptionSelections"
    Compatibility:
      type: object
      required: [score, confidence, reasons]
      properties:
        score:
          type: integer
          minimum: 0
          maximum: 100
        confidence:
          type: number
          minimum: 0
          maximum: 1
        reasons:
          type: array
          items:
            type: string
            enum: [shared_intent, similar_vibe, mutual_age_fit]
    DatezaCompatibility:
      type: [object, "null"]
      description: |
        Pair-specific deterministic DateZA compatibility. null means meaningful
        comparable input weight is below the dateza_v1 publication threshold;
        it does not mean incompatibility. Reason codes reveal no raw answers.
      required: [score, confidence, confidence_level, version, reasons]
      properties:
        score:
          type: integer
          minimum: 0
          maximum: 100
        confidence:
          type: number
          minimum: 0
          maximum: 1
        confidence_level:
          type: string
          enum: [low, medium, high]
        version:
          type: string
          enum: [dateza_v1]
        reasons:
          type: array
          maxItems: 5
          items:
            type: string
            enum:
              - shared_long_term_intent
              - compatible_relationship_goals
              - relationship_goal_mismatch
              - compatible_family_plans
              - family_plan_mismatch
              - shared_no_smoking
              - smoking_lifestyle_mismatch
              - compatible_drinking_style
              - similar_faith_importance
              - similar_social_style
              - compatible_meeting_pace
              - shared_interests
              - shared_languages
              - compatible_communication_style
              - compatible_planning_style
              - similar_travel_style
              - compatible_diet
    ProfileStatus:
      type: object
      description: |
        Viewer-relative status signals, recomputed per request. Present only on the
        discovery feed and the single-profile detail endpoint — NOT on match or
        conversation payloads, which carry the base public profile only.
      required: [verified, online, active_today, new_here, last_active_at, distance_km]
      properties:
        verified:
          type: boolean
          description: |
            The member has proven ownership of at least one contact identifier
            (email or phone). This is CONTACT verification, not photo/identity
            ("it's really them") verification, which does not exist yet — treat it
            as a "reachable" signal rather than a strong trust badge. On profile
            detail, prefer `verification.contact.verified`; this top-level field
            remains as a backward-compatible legacy projection.
        active_today:
          type: boolean
          description: An active brand session was used within the last 24 hours.
        new_here:
          type: boolean
          description: The member's profile was created within the last 7 days.
        online:
          type: boolean
          description: An active brand session was used within the last 10 minutes.
        last_active_at:
          type: [string, "null"]
          format: date-time
          description: |
            Most recent active-session use (ISO8601), or null when the member has
            no active session (never signed in on this brand, revoked, or expired).
        distance_km:
          type: [integer, "null"]
          minimum: 1
          description: |
            Server-calculated whole-kilometre great-circle distance from the
            viewer, floored to 1km (never 0, which would read as exact
            co-location). null unless both members have a location valid under
            the resolved brand policy: DateZA locations persist until replaced or
            removed; HookUs locations must be no older than 24 hours. Clients
            must not calculate distance from coordinates (coordinates are never
            returned).
    HookUsProfileState:
      type: object
      description: Optional HookUs capability state, composed only for HookUs surfaces that enable it.
      required: [hook_tonight_active, hook_state]
      properties:
        hook_tonight_active:
          type: boolean
          description: The member currently has a live Hook Tonight availability state.
        hook_state:
          type: string
          enum: [available, pending, hooked, unavailable]
          description: |
            The viewer's 🔥 Hook button state toward this profile. Deliberately
            does not distinguish declined, expired, or blocked outcomes.
    ProfileWithStatus:
      allOf:
        - $ref: "#/components/schemas/PublicProfile"
        - $ref: "#/components/schemas/ProfileStatus"
        - type: object
          description: HookUs profile detail adds these fields; brands without Hook capabilities omit them.
          properties:
            hook_tonight_active:
              type: boolean
            hook_state:
              type: string
              enum: [available, pending, hooked, unavailable]
    DiscoveryProfile:
      allOf:
        - $ref: "#/components/schemas/PublicProfile"
        - $ref: "#/components/schemas/ProfileStatus"
        - $ref: "#/components/schemas/HookUsProfileState"
        - type: object
          required: [compatibility]
          properties:
            compatibility:
              $ref: "#/components/schemas/Compatibility"
    DailyDiscoveryProfile:
      allOf:
        - $ref: "#/components/schemas/PublicProfile"
        - $ref: "#/components/schemas/ProfileStatus"
        - type: object
          required: [compatibility]
          properties:
            compatibility:
              $ref: "#/components/schemas/DatezaCompatibility"
    DailySelectionMetadata:
      type: object
      required: [allocation_date, daily_limit, count, finalized, refreshes_at]
      properties:
        allocation_date:
          type: string
          format: date
          description: Allocation period date in the configured surface timezone.
        daily_limit:
          type: integer
          minimum: 1
        count:
          type: integer
          minimum: 0
          description: Number of allocated candidates still eligible and safe to deliver.
        finalized:
          type: boolean
          const: true
          description: The period's allocation will not be routinely reranked or refilled.
        refreshes_at:
          type: string
          format: date-time
          description: Start of the next allocation period in the configured timezone.
    DiscoveryResponse:
      type: object
      required: [profiles, next_cursor]
      properties:
        profiles:
          type: array
          items:
            oneOf:
              - $ref: "#/components/schemas/DiscoveryProfile"
              - $ref: "#/components/schemas/DailyDiscoveryProfile"
        next_cursor:
          type: [string, "null"]
        selection:
          $ref: "#/components/schemas/DailySelectionMetadata"
          description: Present only for a stable daily-batch surface.
    FindProfileStatus:
      type: object
      required: [verified, online, active_today, new_here, last_active_at, distance_km]
      properties:
        verified:
          type: boolean
        online:
          type: boolean
        active_today:
          type: boolean
        new_here:
          type: boolean
        last_active_at:
          type: [string, "null"]
          format: date-time
        distance_km:
          type: [integer, "null"]
          minimum: 1
    FindProfile:
      allOf:
        - $ref: "#/components/schemas/PublicProfile"
        - $ref: "#/components/schemas/FindProfileStatus"
        - type: object
          required: [compatibility]
          properties:
            compatibility:
              $ref: "#/components/schemas/DatezaCompatibility"
    FindAllowance:
      type: object
      required: [limit, used, remaining, exhausted, resets_at]
      properties:
        limit:
          type: integer
          minimum: 1
        used:
          type: integer
          minimum: 0
        remaining:
          type: integer
          minimum: 0
        exhausted:
          type: boolean
        resets_at:
          type: string
          format: date-time
          description: Next midnight in Africa/Johannesburg, including its UTC offset.
    FindResponse:
      type: object
      required: [profiles, next_cursor, allowance]
      properties:
        profiles:
          type: array
          items:
            $ref: "#/components/schemas/FindProfile"
        next_cursor:
          type: [string, "null"]
        allowance:
          $ref: "#/components/schemas/FindAllowance"
    ProfileDetail:
      description: |
        The rich single-profile view (GET /profiles/:id): the public profile with
        viewer-relative status, detail-only sections, explicit contact verification,
        and DateZA compatibility where configured. matches_only option groups appear
        under `options` only when an active Match exists.
      allOf:
        - $ref: "#/components/schemas/ProfileWithStatus"
        - type: object
          required: [prompts, interests, verification]
          properties:
            prompts:
              type: array
              items:
                $ref: "#/components/schemas/PromptAnswer"
            interests:
              type: array
              description: Categorized interests derived from the `interests` group's selections.
              items:
                $ref: "#/components/schemas/ProfileInterest"
            verification:
              $ref: "#/components/schemas/ContactVerification"
            compatibility:
              $ref: "#/components/schemas/DatezaCompatibility"
    ProfileDetailResponse:
      type: object
      required: [profile]
      properties:
        profile:
          $ref: "#/components/schemas/ProfileDetail"
    ProfilePreferencesUpdate:
      type: object
      properties:
        min_age:
          type: [integer, "null"]
          minimum: 18
          maximum: 120
        max_age:
          type: [integer, "null"]
          minimum: 18
          maximum: 120
        interested_in:
          type: array
          maxItems: 10
          items:
            type: string
            maxLength: 40
        max_distance_km:
          type: [integer, "null"]
          minimum: 1
          maximum: 500
        country:
          type: [string, "null"]
          pattern: "^[A-Z]{2}$"
        relationship_intent:
          type: [string, "null"]
          maxLength: 80
    ProfilePreferences:
      allOf:
        - $ref: "#/components/schemas/ProfilePreferencesUpdate"
        - type: object
          required: [id, profile_id, brand, interested_in]
          properties:
            id:
              type: integer
            profile_id:
              type: string
              format: uuid
            brand:
              $ref: "#/components/schemas/BrandSummary"
    ProfileLocationUpdate:
      type: object
      required: [latitude, longitude, accuracy_meters, captured_at]
      properties:
        latitude:
          type: number
          minimum: -90
          maximum: 90
          writeOnly: true
        longitude:
          type: number
          minimum: -180
          maximum: 180
          writeOnly: true
        accuracy_meters:
          type: integer
          minimum: 0
          maximum: 100000
        captured_at:
          type: string
          format: date-time
    ProfileLocationState:
      type: object
      required: [configured]
      properties:
        configured:
          type: boolean
        accuracy_meters:
          type: integer
        source:
          type: string
          enum: [device, manual, imported, place]
        captured_at:
          type: string
          format: date-time
        place:
          description: |
            Present only when source is `place`. Never includes coordinates.
            `kind`/`code`/`country_code` are populated by `GET /api/v1/profile/location`;
            the write endpoints (`PUT /profile/location`, `PUT /profile/place`) only
            ever return `id`, `name`, `display_path`.
          type: object
          required: [id, name, display_path]
          properties:
            id:
              type: integer
            kind:
              type: string
              enum: [region, city, locality]
            name:
              type: string
            code:
              type: string
            country_code:
              type: string
            display_path:
              type: string
              description: Human-readable, fine-to-coarse label, e.g. "Sea Point, Cape Town, Western Cape".
    ProfileLocationResponse:
      type: object
      required: [location]
      properties:
        location:
          $ref: "#/components/schemas/ProfileLocationState"
    Place:
      type: object
      required: [id, kind, name, code, has_children]
      properties:
        id:
          type: integer
        kind:
          type: string
          enum: [region, city, locality]
          description: Country-level entries are never listed; DateZA's country_code is implicit.
        name:
          type: string
        code:
          type: string
          description: Stable slug, unique among siblings.
        has_children:
          type: boolean
          description: Whether a more specific level exists to drill into (not every city has cataloged localities).
    LocationSearchResult:
      type: object
      required: [place_id, label, area, city, region, country_code, kind]
      properties:
        place_id:
          type: integer
          description: Directly usable as the place_id body param for PUT /api/v1/profile/place.
        label:
          type: string
          description: Human-readable, fine-to-coarse display label, e.g. "Sea Point, Cape Town, Western Cape".
        area:
          type: string
          description: The matched area's own name (suburb, town, or city name).
        city:
          type: [string, "null"]
          description: Null when the result itself is a region, or no cataloged city ancestor exists.
        region:
          type: [string, "null"]
        country_code:
          type: string
        kind:
          type: string
          enum: [region, city, locality]
          description: Never "country" — a country is never an acceptable dating-area selection.
    ProfilePhoto:
      type: object
      required: [id, profile_id, position, primary, status, visibility, processing_state, deleted_at, image]
      properties:
        id:
          type: integer
        profile_id:
          type: string
          format: uuid
        position:
          type: integer
        primary:
          type: boolean
          description: True for the first kept owner photo in position/id order; derived, never stored separately.
        status:
          type: string
          description: Moderation lifecycle; independent of processing_state and visibility.
          enum: [pending_review, approved, rejected]
        visibility:
          type: string
          enum: [hidden, visible]
        processing_state:
          type: string
          description: |
            Safe-derivative pipeline state. Only `ready` photos are eligible for
            delivery to other users. `image` shows the owner their raw upload
            while `pending`/`processing`, and the safe display derivative once
            `ready`. `failed` marks a permanently unprocessable image.
          enum: [pending, processing, ready, failed]
        deleted_at:
          type: [string, "null"]
          format: date-time
        image:
          oneOf:
            - $ref: "#/components/schemas/ProfilePhotoImage"
            - type: "null"
    ProfilePhotoImage:
      type: object
      required: [filename, content_type, byte_size, url, url_expires_in]
      properties:
        filename:
          type: string
        content_type:
          type: string
          enum: [image/jpeg, image/png, image/webp]
        byte_size:
          type: integer
        url:
          type: string
          description: Short-lived signed retrieval URL (direct from private R2).
        url_expires_in:
          type: integer
          description: Seconds until the signed retrieval URL expires.
    ProfilePhotoUpload:
      type: object
      required: [signed_id, url, headers, expires_in, byte_size_limit, allowed_content_types]
      properties:
        signed_id:
          type: string
          description: Opaque id to submit when attaching the completed upload.
        url:
          type: string
          description: Short-lived presigned PUT URL for the media bytes.
        headers:
          type: object
          additionalProperties:
            type: string
          description: Exact headers to send with the PUT (includes Content-MD5).
        expires_in:
          type: integer
          description: Seconds until the presigned upload URL expires.
        byte_size_limit:
          type: integer
        allowed_content_types:
          type: array
          items:
            type: string
    ProfileConfigurationResponse:
      type: object
      required: [configuration, onboarding]
      properties:
        configuration:
          $ref: "#/components/schemas/ProfileConfiguration"
        onboarding:
          $ref: "#/components/schemas/ProfileOnboardingStatus"
    ProfileConfiguration:
      type: object
      required: [identity_fields, profile_fields, preference_fields, collections, option_groups, prompts, openers]
      properties:
        identity_fields:
          type: array
          description: Private platform-identity fields enabled for this brand's onboarding flow.
          items:
            $ref: "#/components/schemas/ConfiguredField"
        profile_fields:
          type: array
          items:
            $ref: "#/components/schemas/ConfiguredField"
        preference_fields:
          type: array
          items:
            $ref: "#/components/schemas/ConfiguredField"
        collections:
          type: array
          items:
            $ref: "#/components/schemas/ConfiguredCollection"
        option_groups:
          type: array
          items:
            $ref: "#/components/schemas/ConfiguredOptionGroup"
        prompts:
          type: array
          description: Brand-enabled prompt definitions a member may answer.
          items:
            $ref: "#/components/schemas/ConfiguredPrompt"
        openers:
          type: array
          description: >-
            Brand-enabled D8N Opener catalog. Empty for a freeform brand
            (HookUs); a brand whose opener policy requires curation (DateZA)
            lists the entries a sender must choose from for
            `POST /api/v1/profiles/{profile_id}/opener`.
          items:
            $ref: "#/components/schemas/ConfiguredOpener"
    ConfiguredField:
      type: object
      required: [key, label, required, cardinality, input_type, visibility, options]
      properties:
        key:
          type: string
        label:
          type: string
        required:
          type: boolean
        cardinality:
          type: string
          enum: [single, multiple]
        input_type:
          type: string
          enum: [text, textarea, date, integer, select, string_list, language_list]
        visibility:
          type: string
          enum: [owner_only, public_profile]
        options:
          type: array
          description: Controlled choices when the field has a server-owned vocabulary; otherwise empty.
          items:
            type: object
            required: [code, label]
            properties:
              code:
                type: string
              label:
                type: string
    ConfiguredCollection:
      type: object
      required: [key, label, required, minimum_count, maximum_count]
      properties:
        key:
          type: string
        label:
          type: string
        required:
          type: boolean
        minimum_count:
          type: integer
          minimum: 1
        maximum_count:
          type: integer
          minimum: 1
          description: Server-enforced brand-policy maximum for this collection.
    ConfiguredOptionGroup:
      type: object
      required: [key, label, cardinality, max_selections, required, visibility, options]
      properties:
        key:
          type: string
        label:
          type: string
        cardinality:
          type: string
          enum: [single, multiple]
        max_selections:
          type: integer
        required:
          type: boolean
        visibility:
          type: string
          enum: [owner_only, public_profile, matches_only]
        options:
          type: array
          items:
            type: object
            required: [code, label]
            properties:
              code:
                type: string
              label:
                type: string
              category:
                type: [string, "null"]
    ConfiguredPrompt:
      type: object
      required: [key, text]
      properties:
        key:
          type: string
        text:
          type: string
        category:
          type: [string, "null"]
    ConfiguredOpener:
      type: object
      required: [key, text]
      properties:
        key:
          type: string
        text:
          type: string
    IncompleteProfileError:
      allOf:
        - $ref: "#/components/schemas/ErrorResponse"
        - type: object
          required: [completion]
          properties:
            completion:
              type: object
              required: [percent, missing]
              properties:
                percent:
                  type: integer
                missing:
                  type: array
                  items:
                    type: string
    LikeResponse:
      type: object
      required: [liked, matched, match_id, created]
      properties:
        liked:
          const: true
        matched:
          type: boolean
        match_id:
          type: [string, "null"]
          format: uuid
        created:
          type: boolean
    LikeProfile:
      allOf:
        - $ref: "#/components/schemas/PublicProfile"
        - $ref: "#/components/schemas/ProfileStatus"
        - type: object
          properties:
            compatibility:
              $ref: "#/components/schemas/DatezaCompatibility"
              description: >-
                Present only for brands with a pair compatibility strategy
                configured (DateZA today). Omitted entirely — not null — for
                brands without one (HookUs).
    LikeListItem:
      type: object
      required: [liked_at, profile]
      properties:
        liked_at:
          type: string
          format: date-time
        profile:
          $ref: "#/components/schemas/LikeProfile"
    LikeListResponse:
      type: object
      required: [likes, next_cursor]
      properties:
        likes:
          type: array
          items:
            $ref: "#/components/schemas/LikeListItem"
        next_cursor:
          type: [string, "null"]
    PassResponse:
      type: object
      required: [passed, created]
      properties:
        passed:
          const: true
        created:
          type: boolean
    BlockResponse:
      type: object
      required: [blocked, created]
      properties:
        blocked:
          const: true
        created:
          type: boolean
    BlockedProfilesResponse:
      type: object
      required: [blocks]
      properties:
        blocks:
          type: array
          description: Profiles the current member has blocked, newest first.
          items:
            type: object
            required: [profile, blocked_at]
            properties:
              profile:
                type: object
                required: [id, display_name]
                description: Minimum information needed to recognise and unblock the target.
                properties:
                  id:
                    type: string
                    format: uuid
                  display_name:
                    type: [string, "null"]
              blocked_at:
                type: string
                format: date-time
    ReportReason:
      type: string
      description: |
        Stable safety-reason code. Codes are stable and never renumbered; the
        frontend maps them to display labels and may offer a tailored subset per
        target type. `violence_or_threat`, `non_consensual_content`, and
        `impersonation` were added in Reporting V2.
      enum:
        [inappropriate_content, harassment, spam, fake_profile, underage, other,
         violence_or_threat, non_consensual_content, impersonation]
    ReportTargetType:
      type: string
      description: |
        The kind of thing being reported. `profile` reports a person; `message`,
        `profile_media`, and `hook` each report one specific piece of content;
        `conversation` reports the interaction as a whole for pattern-level harm
        no single message captures. New types are added as the underlying
        features ship.
      enum: [profile, message, profile_media, hook, conversation]
    ReportRequest:
      type: object
      required: [reason]
      properties:
        reason:
          $ref: "#/components/schemas/ReportReason"
        note:
          type: [string, "null"]
          maxLength: 2000
          description: Optional free-text detail from the reporter.
    ReportResponse:
      type: object
      required: [reported, created]
      properties:
        reported:
          const: true
        created:
          type: boolean
          description: True when a new report was filed; false when an open report already existed.
    FileReportRequest:
      type: object
      required: [target_type, reason]
      properties:
        target_type:
          $ref: "#/components/schemas/ReportTargetType"
        target_id:
          type: [string, "null"]
          description: |
            Opaque public id of the target content — a message id, photo id,
            Hook id, or conversation id (a profile's public id when
            `target_type` is `profile`). The responsible profile is always
            derived server-side from this target, never supplied by the client.
        reason:
          $ref: "#/components/schemas/ReportReason"
        details:
          type: [string, "null"]
          maxLength: 2000
          description: Optional untrusted reporter context. Normalized and length-bounded.
        block:
          type: boolean
          default: false
          description: |
            When true, the responsible profile is also blocked after the report is
            filed. Both actions are idempotent.
    FileReportResponse:
      type: object
      required: [report, created, blocked]
      properties:
        report:
          type: object
          required: [status]
          properties:
            status:
              type: string
              const: received
              description: |
                Acknowledgement only. Internal moderation status/notes/evidence
                and any offender history are never exposed to the reporter.
        created:
          type: boolean
          description: True when a new report was filed; false when an open report for this target already existed.
        blocked:
          type: boolean
          description: True when `block` was requested and the responsible profile is now blocked.
    ProductNotification:
      type: object
      required: [id, type, title, body, payload, read_at, created_at]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          enum:
            - dateza.welcome
            - dateza.like_received
            - dateza.match_created
            - dateza.opener_received
            - dateza.message_received
          description: Stable semantic type. New types are added compatibly.
        title:
          type: string
        body:
          type: string
        payload:
          oneOf:
            - $ref: "#/components/schemas/EmptyNotificationPayload"
            - $ref: "#/components/schemas/DatingEventNotificationPayload"
          description: |
            Type-controlled, presentation-safe structured data. Empty for
            dateza.welcome. For the D8N dating events (like_received,
            match_created, opener_received, message_received), carries only
            opaque public identifiers — `actor.profile_id` (who did it) and
            `target.type`/`target.id` (what to open). Message notifications also
            carry `target.message_id`, an opaque identifier used to resolve the
            exact message under current authorization. Never latitude/longitude,
            message text, names, or photos; the frontend resolves `actor`/
            `target` through the normal owner-scoped endpoints (profile detail,
            match, conversation), which independently enforce block/suspension/
            closure access. `target.type` is one of `profile`, `match`,
            `opener`, `conversation`.
        read_at:
          type: [string, "null"]
          format: date-time
        created_at:
          type: string
          format: date-time
    EmptyNotificationPayload:
      type: object
      additionalProperties: false
      description: The payload shape for dateza.welcome, which carries no fields.
    DatingEventNotificationPayload:
      type: object
      required: [actor, target]
      additionalProperties: false
      properties:
        actor:
          type: object
          required: [profile_id]
          additionalProperties: false
          description: The profile who performed the action that produced this notification.
          properties:
            profile_id:
              type: string
              format: uuid
        target:
          type: object
          required: [type, id]
          additionalProperties: false
          description: Where the frontend can navigate to act on this notification.
          properties:
            type:
              type: string
              enum: [profile, match, opener, conversation]
            id:
              type: string
              format: uuid
            message_id:
              type: string
              format: uuid
              description: Present only when target.type is conversation.
    NotificationListResponse:
      type: object
      required: [notifications, unread_count]
      properties:
        notifications:
          type: array
          maxItems: 50
          items:
            $ref: "#/components/schemas/ProductNotification"
        unread_count:
          type: integer
          minimum: 0
    NotificationResponse:
      type: object
      required: [notification]
      properties:
        notification:
          $ref: "#/components/schemas/ProductNotification"
    MarkAllNotificationsReadResponse:
      type: object
      required: [marked_read, unread_count]
      properties:
        marked_read:
          type: integer
          minimum: 0
        unread_count:
          const: 0
    NotificationPreferences:
      type: object
      required: [product_email_enabled, push_enabled]
      properties:
        product_email_enabled:
          type: boolean
          description: Whether D8N product notifications (e.g. match_created) may be emailed. In-app delivery is unaffected.
        push_enabled:
          type: boolean
          description: Whether D8N product notifications may be pushed to a registered device. In-app delivery is unaffected.
    NotificationPreferencesResponse:
      type: object
      required: [preferences]
      properties:
        preferences:
          $ref: "#/components/schemas/NotificationPreferences"
    NotificationPreferencesUpdate:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        product_email_enabled:
          type: boolean
        push_enabled:
          type: boolean
    Match:
      type: object
      required: [id, matched_at, profile]
      properties:
        id:
          type: string
          format: uuid
        matched_at:
          type: string
          format: date-time
        profile:
          $ref: "#/components/schemas/PublicProfile"
    MatchListResponse:
      type: object
      required: [matches, next_cursor]
      properties:
        matches:
          type: array
          items:
            $ref: "#/components/schemas/Match"
        next_cursor:
          type: [string, "null"]
    Conversation:
      type: object
      required: [id, match_id, status, created_at, profile, last_message]
      properties:
        id:
          type: string
          format: uuid
        match_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [active, closed]
        created_at:
          type: string
          format: date-time
        profile:
          $ref: "#/components/schemas/PublicProfile"
        last_message:
          description: Preview of the newest message, or null for an empty conversation.
          oneOf:
            - $ref: "#/components/schemas/MessagePreview"
            - type: "null"
    MessagePreview:
      type: object
      required: [id, sender_id, body, created_at]
      properties:
        id:
          type: string
          format: uuid
        sender_id:
          type: [string, "null"]
          format: uuid
          description: Public profile id of the author.
        body:
          type: string
        created_at:
          type: string
          format: date-time
    Message:
      type: object
      required: [id, conversation_id, sender_id, body, attachments, created_at]
      properties:
        id:
          type: string
          format: uuid
        conversation_id:
          type: string
          format: uuid
        sender_id:
          type: string
          format: uuid
          description: |
            Public profile id of the author. The client identifies its own
            messages by comparing against the counterpart profile id on the
            conversation (there are exactly two participants).
        body:
          type: [string, "null"]
          description: Null for a media-only message (D8N Chat Media). A message always has a body, an attachment, or both.
        attachments:
          type: array
          description: Image/video attachments (D8N Chat Media), ordered by position. Empty for a text-only message.
          items:
            $ref: "#/components/schemas/MessageAttachment"
        created_at:
          type: string
          format: date-time
    MessageAttachment:
      type: object
      required: [id, media_kind, processing_state, deleted, content_type, byte_size, width, height, duration_seconds]
      properties:
        id:
          type: string
          format: uuid
        media_kind:
          type: string
          enum: [image, video]
        processing_state:
          type: string
          description: |
            `pending`/`processing` until the safe delivery rendition exists
            (image: re-encoded/EXIF-stripped derivative; video: structural
            validation — see Media::VideoContainerValidator). Only `ready`
            attachments carry `view_url`/`download_url`/`poster_url`; the
            recipient never sees a broken half-processed attachment as ready.
            `failed` is terminal (corrupt/unsupported upload).
          enum: [pending, processing, ready, failed]
        deleted:
          type: boolean
          description: True once the sender has deleted this attachment ("delete for everyone"). No delivery URLs are present when true.
        content_type:
          type: [string, "null"]
        byte_size:
          type: [integer, "null"]
        width:
          type: [integer, "null"]
          description: Image only; null for video (no server-side frame decode is available — see ticket's video processing notes).
        height:
          type: [integer, "null"]
        duration_seconds:
          type: [number, "null"]
          description: Video only, best-effort from the container's `mvhd` box; null if unavailable.
        view_url:
          type: string
          description: Short-lived signed URL for inline view/play (the safe rendition, never the full-quality original). Present only when `processing_state` is `ready` and not deleted.
        download_url:
          type: string
          description: Short-lived signed URL with a `Content-Disposition:attachment` header, serving the original full-quality bytes. Present only when `processing_state` is `ready` and not deleted.
        poster_url:
          type: string
          description: Video only. Short-lived signed URL for a client-supplied poster/thumbnail image, if one was provided at send time. Never generated server-side (no video frame decoder in this deployment).
    MessageListResponse:
      type: object
      required: [messages, next_cursor]
      properties:
        messages:
          type: array
          description: Newest-first page of messages.
          items:
            $ref: "#/components/schemas/Message"
        next_cursor:
          type: [string, "null"]
    SendMessageRequest:
      type: object
      description: >-
        At least one of `body` or `attachment_uploads` is required; a message
        with neither is rejected (`message_blank`).
      properties:
        body:
          type: string
          minLength: 1
          maxLength: 2000
          description: Plain-text message. NFC-normalized, Unicode preserved. Optional when at least one attachment is present.
        attachment_uploads:
          type: array
          maxItems: 4
          description: >-
            Already-uploaded, verified blobs to finalize into this message
            (D8N Chat Media). Each `signed_id` comes from a prior
            `POST /api/v1/conversations/{conversation_id}/attachments/uploads`
            call whose bytes have already been PUT to the returned URL. "Attach"
            and "send" are the same operation — there is no separate finalize step.
          items:
            $ref: "#/components/schemas/AttachmentUploadRef"
    AttachmentUploadRef:
      type: object
      required: [signed_id, media_kind]
      properties:
        signed_id:
          type: string
          description: Opaque id returned by the upload-authorization endpoint.
        media_kind:
          type: string
          enum: [image, video]
        poster_signed_id:
          type: string
          description: Optional. Signed id of a separately-uploaded (image, media_kind=image) poster/thumbnail for a video attachment.
    SendMessageResponse:
      type: object
      required: [message]
      properties:
        message:
          $ref: "#/components/schemas/Message"
    MessageAttachmentUpload:
      type: object
      required: [signed_id, url, headers, expires_in, media_kind, byte_size_limit, allowed_content_types]
      properties:
        signed_id:
          type: string
          description: Opaque id to submit as `attachment_uploads[].signed_id` when sending the message.
        url:
          type: string
          description: Short-lived presigned PUT URL for the media bytes.
        headers:
          type: object
          additionalProperties:
            type: string
          description: Exact headers to send with the PUT (includes Content-MD5).
        expires_in:
          type: integer
          description: Seconds until the presigned upload URL expires.
        media_kind:
          type: string
          enum: [image, video]
        byte_size_limit:
          type: integer
          description: >-
            Technical abuse ceiling for this media kind (video: a generous
            infrastructure protection, not a product-facing video length/size
            limit — see Messaging::MessageAttachmentUpload).
        allowed_content_types:
          type: array
          items:
            type: string
    CreateMessageAttachmentUploadRequest:
      type: object
      required: [media_kind, content_type, byte_size, checksum]
      properties:
        media_kind:
          type: string
          enum: [image, video]
        content_type:
          type: string
          enum: [image/jpeg, image/png, image/webp, video/mp4, video/quicktime]
        byte_size:
          type: integer
          minimum: 1
        checksum:
          type: string
          description: Base64-encoded MD5 digest of the file bytes.
        filename:
          type: string
    DeleteMessageAttachmentResponse:
      type: object
      required: [attachment]
      properties:
        attachment:
          type: object
          required: [id, deleted]
          properties:
            id:
              type: string
              format: uuid
            deleted:
              type: boolean
    HookTonightActivateRequest:
      type: object
      description: |
        Optional activation body. V1 accepts a single availability `intent`;
        omitting the body activates with the default.
      properties:
        intent:
          type: string
          enum: [open_to_meeting]
          default: open_to_meeting
          description: The availability intent. Unsupported values yield `invalid_intent`.
    HookTonightState:
      type: object
      description: |
        Current Hook Tonight availability, authoritative and clock-checked. When
        `active` is false, `expires_at` and `intent` are null.
      required: [active, expires_at, intent]
      properties:
        active:
          type: boolean
          description: Whether the member is currently live in the Hook Tonight pool.
        expires_at:
          type: [string, "null"]
          format: date-time
          description: When the current availability lapses, or null when inactive.
        intent:
          type: [string, "null"]
          enum: [open_to_meeting, null]
          description: The active availability intent, or null when inactive.
    SendHookRequest:
      type: object
      required: [message]
      properties:
        message:
          type: string
          minLength: 1
          maxLength: 2000
          description: |
            The opener the sender chose to submit (the pre-filled default or an
            edited version). Plain text, non-blank, NFC-normalized, Unicode
            preserved. Same constraints as a chat message.
    SendHookResponse:
      type: object
      required: [hook]
      properties:
        hook:
          $ref: "#/components/schemas/HookAcknowledgement"
    HookAcknowledgement:
      type: object
      description: |
        Minimal sender-facing acknowledgement. The sender learns only that their
        opener is pending and when it lapses — never anything the recipient does.
      required: [id, status, created_at, expires_at]
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
          enum: [pending]
        created_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
          description: When the pending Hook expires if the recipient never engages.
    ReceivedHook:
      type: object
      description: One received Hook in the recipient inbox.
      required: [id, message, created_at, expires_at, sender]
      properties:
        id:
          type: string
          format: uuid
        message:
          type: string
          description: The sender's opener.
        created_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
        sender:
          $ref: "#/components/schemas/PublicProfile"
    HookInboxResponse:
      type: object
      required: [hooks, next_cursor]
      properties:
        hooks:
          type: array
          description: Newest-first page of live received Hooks.
          items:
            $ref: "#/components/schemas/ReceivedHook"
        next_cursor:
          type: [string, "null"]
    HookReplyRequest:
      type: object
      required: [message]
      properties:
        message:
          type: string
          minLength: 1
          maxLength: 2000
          description: The recipient's reply text. Same constraints as a chat message.
    HookReplyResponse:
      type: object
      description: The unlocked conversation plus the recipient's stored reply message.
      required: [conversation, message]
      properties:
        conversation:
          $ref: "#/components/schemas/Conversation"
        message:
          $ref: "#/components/schemas/Message"
    SendOpenerRequest:
      type: object
      required: [opener_key]
      properties:
        opener_key:
          type: string
          description: |
            The stable key of one entry from the brand's curated opener catalog
            (`GET /api/v1/profile/configuration` → `openers`). D8N Opener brands
            are catalog-only — there is no freeform text field; the resolved
            catalog `text` is what gets sent.
    SendOpenerResponse:
      type: object
      required: [opener]
      properties:
        opener:
          $ref: "#/components/schemas/HookAcknowledgement"
    OpenerInboxResponse:
      type: object
      required: [openers, next_cursor]
      properties:
        openers:
          type: array
          description: Newest-first page of live received D8N Openers.
          items:
            $ref: "#/components/schemas/ReceivedHook"
        next_cursor:
          type: [string, "null"]
    AdminProfilePhotoModeration:
      type: object
      required: [id, profile_id, position, status, visibility, processing_state]
      properties:
        id:
          type: string
          format: uuid
        profile_id:
          type: string
          format: uuid
        position:
          type: integer
          minimum: 0
        status:
          type: string
          enum: [approved, rejected]
        visibility:
          type: string
          enum: [hidden, visible]
        processing_state:
          type: string
          enum: [pending, processing, ready, failed]
    AdminProfilePhotoQueueResponse:
      type: object
      required: [photos]
      properties:
        photos:
          type: array
          items:
            $ref: "#/components/schemas/AdminProfilePhotoQueueEntry"
    AdminProfilePhotoQueueEntry:
      type: object
      required: [id, profile_id, position, created_at, image]
      properties:
        id:
          type: string
          format: uuid
        profile_id:
          type: string
          format: uuid
        position:
          type: integer
          minimum: 0
        created_at:
          type: string
          format: date-time
        image:
          type: [object, "null"]
          description: Short-lived signed URL to the safe (EXIF-stripped) display derivative, or null if processing has not completed.
          required: [content_type, url, url_expires_in]
          properties:
            content_type:
              type: string
            url:
              type: string
              format: uri
            url_expires_in:
              type: integer
    AdminReportParty:
      description: Minimal identity of a report participant, or null if unresolved.
      oneOf:
        - type: object
          required: [id, display_name]
          properties:
            id:
              type: string
              format: uuid
            display_name:
              type: [string, "null"]
        - type: "null"
    AdminReport:
      type: object
      required:
        [id, status, reason, target_type, evidence, reporter, reported, note,
         resolution_note, reviewed_by_admin_user_id, reviewed_at, created_at, updated_at]
      properties:
        id:
          type: integer
        status:
          type: string
          enum: [open, reviewing, actioned, dismissed]
        reason:
          $ref: "#/components/schemas/ReportReason"
        target_type:
          $ref: "#/components/schemas/ReportTargetType"
        evidence:
          type: object
          additionalProperties: true
          description: |
            Minimal, immutable moderation snapshot captured when the report was
            filed (e.g. message/opener text, photo identity + lifecycle). Empty
            for a plain profile report. Survives later deletion of the target.
            Moderator-only; never contains R2 keys, storage paths, or URLs.
        reporter:
          $ref: "#/components/schemas/AdminReportParty"
        reported:
          description: The responsible profile, derived server-side from the target.
          $ref: "#/components/schemas/AdminReportParty"
        note:
          type: [string, "null"]
          description: Reporter-supplied context.
        resolution_note:
          type: [string, "null"]
          description: Moderator's short decision note.
        reviewed_by_admin_user_id:
          type: [integer, "null"]
        reviewed_at:
          type: [string, "null"]
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    AdminReportListResponse:
      type: object
      required: [reports, next_cursor]
      properties:
        reports:
          type: array
          items:
            $ref: "#/components/schemas/AdminReport"
        next_cursor:
          type: [string, "null"]
    AdminReportResponse:
      type: object
      required: [report]
      properties:
        report:
          $ref: "#/components/schemas/AdminReport"
    UpdateReportRequest:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [reviewing, actioned, dismissed, open]
          description: Target lifecycle status. Server enforces valid transitions.
        note:
          type: [string, "null"]
          maxLength: 2000
          description: Optional short moderator decision note.
    SuspendProfileRequest:
      type: object
      properties:
        reason:
          type: [string, "null"]
          maxLength: 500
          description: Optional short moderator reason (audited, never exposed publicly).
        report_id:
          type: [integer, "null"]
          description: Optional id of the report that motivated the enforcement (same brand).
    AdminEnforcement:
      type: object
      required:
        [id, state, profile_id, reason, report_id, admin_user_id,
         reverted_by_admin_user_id, created_at, reverted_at]
      properties:
        id:
          type: integer
        state:
          type: string
          enum: [active, reverted]
          description: The enforcement's own state, distinct from account/report status.
        profile_id:
          type: [string, "null"]
          format: uuid
        reason:
          type: [string, "null"]
        report_id:
          type: [integer, "null"]
        admin_user_id:
          type: integer
        reverted_by_admin_user_id:
          type: [integer, "null"]
        created_at:
          type: string
          format: date-time
        reverted_at:
          type: [string, "null"]
          format: date-time
    AdminEnforcementResponse:
      type: object
      required: [enforcement]
      properties:
        enforcement:
          $ref: "#/components/schemas/AdminEnforcement"
    HqAnalyticsOverviewResponse:
      type: object
      required: [overview]
      properties:
        overview:
          type: object
          required:
            [brand, generated_at, time_zone, signups_today, signups_this_week,
             signups_this_month, active_today, active_7d, active_30d, gender_split,
             total_registered_members]
          properties:
            brand:
              type: string
            generated_at:
              type: string
              format: date-time
            time_zone:
              type: string
              description: IANA timezone used for rollup boundaries.
            signups_today:
              type: integer
            signups_this_week:
              type: integer
              description: Sunday through Saturday in the brand timezone.
            signups_this_month:
              type: integer
              description: From the first day of the calendar month in the brand timezone.
            active_today:
              type: integer
            active_7d:
              type: integer
            active_30d:
              type: integer
            gender_split:
              type: object
              required: [woman, man, other, unknown]
              properties:
                woman:
                  type: integer
                man:
                  type: integer
                other:
                  type: integer
                unknown:
                  type: integer
            total_registered_members:
              type: integer
    HqTrustSafetyOverviewResponse:
      type: object
      required: [overview]
      properties:
        overview:
          type: object
          required: [brand, generated_at, reports, enforcements]
          properties:
            brand:
              type: string
            generated_at:
              type: string
              format: date-time
            reports:
              type: object
              required:
                [total, by_status, awaiting_decision, oldest_open_report_at,
                 oldest_open_report_age_seconds, by_reason, by_target_type,
                 sla_status, overdue]
              properties:
                total:
                  type: integer
                by_status:
                  type: object
                  description: Stable keys `open`, `reviewing`, `actioned`, and `dismissed`; absent states are returned as zero.
                  additionalProperties:
                    type: integer
                awaiting_decision:
                  type: integer
                  description: Reports whose status is `open` or `reviewing`.
                oldest_open_report_at:
                  type: [string, "null"]
                  format: date-time
                oldest_open_report_age_seconds:
                  type: [integer, "null"]
                by_reason:
                  type: object
                  description: Stable keys for every current Report reason; absent reasons are returned as zero.
                  additionalProperties:
                    type: integer
                by_target_type:
                  type: object
                  description: Stable keys for every current Report target type; absent types are returned as zero.
                  additionalProperties:
                    type: integer
                sla_status:
                  type: string
                  enum: [not_configured]
                overdue:
                  type: [integer, "null"]
                  description: Null until an approved Trust & Safety SLA policy exists.
            enforcements:
              type: object
              required: [total, active]
              properties:
                total:
                  type: integer
                active:
                  type: integer
    HqRepeatOffender:
      type: object
      required:
        [profile_id, display_name, member_360_lookup, report_count,
         awaiting_decision_count, latest_report_at]
      properties:
        profile_id:
          type: string
          format: uuid
        display_name:
          type: [string, "null"]
        member_360_lookup:
          type: [string, "null"]
          format: uuid
        report_count:
          type: integer
          minimum: 2
        awaiting_decision_count:
          type: integer
        latest_report_at:
          type: string
          format: date-time
    HqRepeatOffenderListResponse:
      type: object
      required: [repeat_offenders, minimum_reports, truncated]
      properties:
        repeat_offenders:
          type: array
          items:
            $ref: "#/components/schemas/HqRepeatOffender"
        minimum_reports:
          type: integer
          enum: [2]
        truncated:
          type: boolean
    HqMemberSummary:
      type: object
      required: [user_id, profile_id, brand, membership_status]
      properties:
        user_id:
          type: integer
        profile_id:
          type: [string, "null"]
          format: uuid
        brand:
          type: string
        membership_status:
          type: string
          enum: [active, suspended, left, deactivated]
    HqIdentifier:
      type: object
      required: [kind, value, verified_at, last_seen_at]
      properties:
        kind:
          type: string
          enum: [email, phone]
        value:
          type: string
        verified_at:
          type: [string, "null"]
          format: date-time
        last_seen_at:
          type: [string, "null"]
          format: date-time
    HqSession:
      type: object
      required: [device_name, ip_address, last_used_at, expires_at, revoked_at]
      properties:
        device_name:
          type: [string, "null"]
        ip_address:
          type: [string, "null"]
        last_used_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
        revoked_at:
          type: [string, "null"]
          format: date-time
    HqIdentitySection:
      type: object
      required:
        [user_id, user_status, first_name, last_name, user_created_at, membership_status, member_since,
         identifiers, recent_sessions]
      properties:
        user_id:
          type: integer
        user_status:
          type: string
          enum: [active, suspended, closed]
        first_name:
          type: [string, "null"]
        last_name:
          type: [string, "null"]
        user_created_at:
          type: string
          format: date-time
        membership_status:
          type: string
          enum: [active, suspended, left, deactivated]
        member_since:
          type: string
          format: date-time
        identifiers:
          type: array
          description: Up to the 10 oldest kept email/phone identifiers. Never includes oauth or device-fingerprint identifiers.
          items:
            $ref: "#/components/schemas/HqIdentifier"
        recent_sessions:
          type: array
          description: Up to the 5 most recently used sessions for this brand. No token digests.
          items:
            $ref: "#/components/schemas/HqSession"
    HqProfileSection:
      type: object
      required: [exists]
      properties:
        exists:
          type: boolean
          description: False when this member has no Profile on this brand yet — every other property is absent in that case.
        public_id:
          type: string
          format: uuid
        display_name:
          type: [string, "null"]
        status:
          type: string
          enum: [draft, active, suspended]
        visibility:
          type: string
          enum: [hidden, visible]
        gender:
          type: [string, "null"]
        birthdate:
          type: [string, "null"]
          format: date
        country_code:
          type: [string, "null"]
        city:
          type: [string, "null"]
        created_at:
          type: string
          format: date-time
        onboarding_state:
          type: string
          enum: [profile_required, profile_incomplete, ready_to_publish, complete, profile_suspended]
        onboarding_next_step:
          type: [string, "null"]
        onboarding_completion_percent:
          type: integer
        photo_count:
          type: integer
        photos:
          type: array
          description: Up to the 20 most recent kept photos, ordered by position. No image URLs.
          items:
            type: object
            required: [id, position, status, visibility, processing_state]
            properties:
              id:
                type: string
              position:
                type: integer
              status:
                type: string
                enum: [pending_review, approved, rejected]
              visibility:
                type: string
                enum: [hidden, visible]
              processing_state:
                type: string
                enum: [pending, processing, ready, failed]
        preference:
          type: object
          nullable: true
          properties:
            min_age:
              type: [integer, "null"]
            max_age:
              type: [integer, "null"]
            max_distance_km:
              type: [integer, "null"]
            relationship_intent:
              type: [string, "null"]
            interested_in:
              type: array
              items:
                type: string
            country:
              type: [string, "null"]
    HqProductSection:
      type: object
      required:
        [likes_given, likes_received, matches_active, hooks_sent, hooks_received, hooks_live_sent,
         hooks_live_received, hook_tonight_live, conversations_count, recent_conversations,
         blocks_given, blocks_received]
      properties:
        likes_given:
          type: integer
        likes_received:
          type: integer
        matches_active:
          type: integer
        hooks_sent:
          type: integer
        hooks_received:
          type: integer
        hooks_live_sent:
          type: integer
        hooks_live_received:
          type: integer
        hook_tonight_live:
          type: boolean
        conversations_count:
          type: integer
        recent_conversations:
          type: array
          description: Up to the 5 most recent conversations. No message content.
          items:
            type: object
            required: [id, status, created_at]
            properties:
              id:
                type: string
                format: uuid
              status:
                type: string
                enum: [active, closed]
              created_at:
                type: string
                format: date-time
        blocks_given:
          type: integer
        blocks_received:
          type: integer
    HqCommsSection:
      type: object
      required: [delivery_counts_by_status, delivery_counts_by_channel, recent_deliveries]
      properties:
        delivery_counts_by_status:
          type: object
          description: Counts over the 200 most recent NotificationDelivery rows for this member, keyed by status.
          additionalProperties:
            type: integer
        delivery_counts_by_channel:
          type: object
          additionalProperties:
            type: integer
        recent_deliveries:
          type: array
          description: The 10 most recent deliveries.
          items:
            type: object
            required: [channel, status, provider, sent_at, failed_at, error_code, created_at]
            properties:
              channel:
                type: string
                enum: [sms, email, push, whatsapp, in_app]
              status:
                type: string
                enum: [pending, sent, failed, skipped, processing]
              provider:
                type: string
              sent_at:
                type: [string, "null"]
                format: date-time
              failed_at:
                type: [string, "null"]
                format: date-time
              error_code:
                type: [string, "null"]
              created_at:
                type: string
                format: date-time
    HqSafetySection:
      type: object
      required:
        [reports_filed_count, reports_received_count, recent_reports, active_enforcement,
         enforcement_count, account_closure]
      properties:
        reports_filed_count:
          type: integer
        reports_received_count:
          type: integer
        recent_reports:
          type: array
          description: Up to the 5 most recent reports naming this member in either direction. No evidence/note — see GET /api/v1/admin/reports/{id} for full detail.
          items:
            type: object
            required: [id, status, reason, target_type, direction, created_at]
            properties:
              id:
                type: integer
              status:
                type: string
                enum: [open, reviewing, actioned, dismissed]
              reason:
                type: string
              target_type:
                type: string
                enum: [profile, message, profile_media, hook, conversation]
              direction:
                type: string
                enum: [filed, received]
              created_at:
                type: string
                format: date-time
        active_enforcement:
          allOf:
            - $ref: "#/components/schemas/AdminEnforcement"
          nullable: true
        enforcement_count:
          type: integer
        account_closure:
          type: object
          nullable: true
          properties:
            media_purge_state:
              type: string
              enum: [pending, completed, failed]
            created_at:
              type: string
              format: date-time
    HqActivitySection:
      type: object
      required: [last_login_at, recent_auth_attempts, recent_security_events]
      properties:
        last_login_at:
          type: [string, "null"]
          format: date-time
        recent_auth_attempts:
          type: array
          description: Up to the 5 most recent auth attempts. Full history via GET .../auth_attempts.
          items:
            type: object
            required: [kind, result, ip_address, created_at]
            properties:
              kind:
                type: string
                enum: [password, email_otp, phone_otp, oauth, webauthn, recovery_code]
              result:
                type: string
                enum: [succeeded, failed, throttled, locked]
              ip_address:
                type: [string, "null"]
              created_at:
                type: string
                format: date-time
        recent_security_events:
          type: array
          description: Up to the 5 most recent security events. Full history via GET .../security_events.
          items:
            type: object
            required: [event_type, severity, created_at]
            properties:
              event_type:
                type: string
              severity:
                type: string
                enum: [info, warning, high, critical]
              created_at:
                type: string
                format: date-time
    HqMemberDirectoryEntry:
      type: object
      required:
        [user_id, profile_id, display_name, user_status, membership_status,
         profile_status, profile_visibility, joined_at, user_created_at,
         reports_received_count, pending_photo_count, active_enforcement]
      properties:
        user_id:
          type: integer
        profile_id:
          type: [string, "null"]
        display_name:
          type: [string, "null"]
        user_status:
          type: string
          enum: [active, suspended, closed]
        membership_status:
          type: string
          enum: [active, suspended, left, deactivated]
        profile_status:
          type: [string, "null"]
          enum: [draft, active, suspended, null]
        profile_visibility:
          type: [string, "null"]
          enum: [hidden, visible, null]
        joined_at:
          type: string
          format: date-time
        user_created_at:
          type: string
          format: date-time
        reports_received_count:
          type: integer
        pending_photo_count:
          type: integer
        active_enforcement:
          type: boolean
    HqMemberDirectoryListResponse:
      type: object
      required: [members, next_cursor]
      properties:
        members:
          type: array
          items:
            $ref: "#/components/schemas/HqMemberDirectoryEntry"
        next_cursor:
          type: [string, "null"]
    HqMember360Response:
      type: object
      required: [member, sections]
      properties:
        member:
          $ref: "#/components/schemas/HqMemberSummary"
        sections:
          type: object
          required: [identity, profile, product, comms, safety, activity]
          properties:
            identity:
              $ref: "#/components/schemas/HqIdentitySection"
            profile:
              $ref: "#/components/schemas/HqProfileSection"
            product:
              $ref: "#/components/schemas/HqProductSection"
            comms:
              $ref: "#/components/schemas/HqCommsSection"
            safety:
              $ref: "#/components/schemas/HqSafetySection"
            activity:
              $ref: "#/components/schemas/HqActivitySection"
    HqSecurityEvent:
      type: object
      required: [id, event_type, severity, metadata, ip_address, created_at]
      properties:
        id:
          type: integer
        event_type:
          type: string
        severity:
          type: string
          enum: [info, warning, high, critical]
        metadata:
          type: object
          description: Opaque ids/status only, per this codebase's existing SecurityEvent convention — never raw message/report content.
        ip_address:
          type: [string, "null"]
        created_at:
          type: string
          format: date-time
    HqSecurityEventListResponse:
      type: object
      required: [security_events, next_cursor]
      properties:
        security_events:
          type: array
          items:
            $ref: "#/components/schemas/HqSecurityEvent"
        next_cursor:
          type: [string, "null"]
    HqAuthAttempt:
      type: object
      required: [id, kind, result, identifier, ip_address, created_at]
      properties:
        id:
          type: integer
        kind:
          type: string
          enum: [password, email_otp, phone_otp, oauth, webauthn, recovery_code]
        result:
          type: string
          enum: [succeeded, failed, throttled, locked]
        identifier:
          type: string
          description: The raw identifier string supplied at attempt time (as typed, not necessarily normalized).
        ip_address:
          type: [string, "null"]
        created_at:
          type: string
          format: date-time
    HqAuthAttemptListResponse:
      type: object
      required: [auth_attempts, next_cursor]
      properties:
        auth_attempts:
          type: array
          items:
            $ref: "#/components/schemas/HqAuthAttempt"
        next_cursor:
          type: [string, "null"]
    HqEnforcementListResponse:
      type: object
      required: [enforcements, next_cursor]
      properties:
        enforcements:
          type: array
          items:
            $ref: "#/components/schemas/AdminEnforcement"
        next_cursor:
          type: [string, "null"]
    HqDiscoveryDiagnosticResponse:
      type: object
      required: [eligible, ineligibility_reason, stages]
      properties:
        eligible:
          type: boolean
          description: Whether this member is currently eligible to see discovery at all.
        ineligibility_reason:
          type: [string, "null"]
          description: Set only when `eligible` is false (e.g. `profile_unavailable`, `discovery_not_configured`).
        stages:
          type: array
          description: Empty when `eligible` is false. Otherwise one entry per discovery funnel stage, in order.
          items:
            type: object
            required: [stage, description, candidate_count]
            properties:
              stage:
                type: string
                enum: [visible_active_profiles, reciprocal_gender_age_distance, final_eligible_candidates]
              description:
                type: string
              candidate_count:
                type: integer
    StartConversationResponse:
      type: object
      required: [conversation, created]
      properties:
        conversation:
          $ref: "#/components/schemas/Conversation"
        created:
          type: boolean
    ConversationListResponse:
      type: object
      required: [conversations, next_cursor]
      properties:
        conversations:
          type: array
          items:
            $ref: "#/components/schemas/Conversation"
        next_cursor:
          type: [string, "null"]
